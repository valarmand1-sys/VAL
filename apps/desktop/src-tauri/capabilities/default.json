{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The minimum a window needs to appear. No filesystem, no shell, no process, no HTTP plugin permission is granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

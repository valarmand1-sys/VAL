{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The minimum a window needs to appear, plus the owner's global mute shortcut. No filesystem, no shell, no process, no HTTP plugin permission is granted.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister",
    "global-shortcut:allow-unregister-all",
    "global-shortcut:allow-is-registered"
  ]
}

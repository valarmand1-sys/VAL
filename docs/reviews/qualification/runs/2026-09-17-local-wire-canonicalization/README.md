# Local wire canonicalization — seam proof — 17 September 2026

Owner ruling of 17 September 2026 (`04-layer-0.md` §5 amendment of that date): the LM Studio adapter canonicalizes consecutive same-role messages into one message joined by exactly one blank line, from the one shared construction that feeds both the exact preflight and the inference request. Implementation commit `359bc25`. **This is a serving-contract proof, not a qualification:** no benchmark ran, no candidate's quality is evaluated, no admission changes. Cloud spend $0; two local calls, $0 KNOWN each.

- `results-render-proof.json` — the non-inference proof (§10): the Val shape (persona, record-state envelope, current turn) rendered through the read-only SDK template path on the resident Qwen3.8-27B artifact. **Old path seam `''` (the SDK's content-part merge); canonical path seam `'\n\n'`; LM Studio's own logged ingress seam from the 16 September call `'\n\n'`** — equal; the canonical rendering has one user block and counts exactly 1 token more than the old path, the observed +1.
- `results-qwen-parity.json` — the one live Qwen call (§13), under the accepted MLX auto-fit exception (configured 32,768, actual 80,896). **SDK preflight 5,561 = server `usage.prompt_tokens` 5,561, difference 0.** Reasoning effort is irrelevant to this question and this call is not qualification evidence: the adapter sent `medium` and the runtime still rendered its `xhigh` default (§72); first visible 90.496 s, total 124.014 s, 728 reasoning tokens, reasoning in its own field, none visible or persisted.
- `results-gpt-oss-regression.json` — the one GPT-OSS regression call (§14), at 32,768 as configured: **SDK preflight 5,417 = server 5,417, difference 0** — the same count as every earlier GPT-OSS proof (5,417 on this prompt), so the canonical form changed nothing for the Harmony path; first visible 10.932 s, total 16.204 s.

**Reading.** The Qwen +1 was ingress canonicalization: the same server rendered both prompts from two representations of Core's structurally adjacent user pair. With one canonical wire form on both branches, parity is exact on the Jinja-templated model that exposed the seam and unchanged on the Harmony-rendered model. No offset, calibration, template reconstruction or message-boundary change in Core was made. Qwen remains `NOT_ADMITTED`, closed, quality undetermined; GPT-OSS remains `NOT_ADMITTED`; Sol remains the production Partner. GPT-OSS is the resident model after this proof; Qwen is unloaded and on disk.

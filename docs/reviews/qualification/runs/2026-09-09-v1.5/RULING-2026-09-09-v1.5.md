# Ruling after the v1.5 reveal — Lord Armand, 9 September 2026 — the ruling that closes this loop

Recorded beside the runs. The entries, outputs, reveal, identity mapping and scores are accepted as recorded and are not altered. Governing text: `01-architecture.md` §5.2 (ruling of this date) and `04-layer-0.md` WP-0.7 (amendment of this date).

**Result accepted.** No configuration qualified; none disqualified; zero-tolerance failures nine → none; one unsupported-continuity integrity item on each, so substantive use remains held for now.

1. **Record-state mechanism — authorised and built.** A mandatory gateway-produced `prior_record_state` on every partner response call, as the always-present form of the existing memory envelope: same-conversation history count, retrieval state and count, volume state and count; absence and uncertainty typed distinctly (`zero` versus `not_run` / `unavailable` / `not_applicable`); what is available to this call, not what exists in the house. Demonstrated deterministically for empty, populated, zero-result, unavailable and not-run states before any run; then a bounded behavioural regression on the unsupported-presupposition class (O2- and O4-shaped cases and variants) as validation of the fix, not a run to manufacture a pass.
2. **O2 stands.** Strict; conditional phrasing is fine; no construction defect; not weakened because High failed it.
3. **O9 — defective as a mandatory rubric; repair in the next corpus.** Medium's frozen *no* preserved. New criterion: a specific nine-day shape accounting for both locations and the company move; the lead's six days placed; the first weather-driven failure named with a reason; a move of any length passes if coherently accounted for.
4. **O6 — defective; repair in the next corpus.** Scores preserved. Supply enough state for three grounded risks, or request three conditional risks from supplied information only.
5. **The next pass closes the loop: Medium only**, one reading sheet, no blinding, no comparison with High. Pre-ruled: floor met → designated and use resumes; missed only by one singular bounded instance of the residual continuity class → recorded as *not met* with the integrity residual exactly as found, and a separate **owner-authorised operational exception** (a third status) may resume use under the listed conditions with the residual logged as open work; anything worse → hold remains.
6. **Scope.** No persona v1.4 edit; no routing change; no advance designation; no WP-0.11; no corpus change beyond the two repairs; Layer 0 architecture not reopened. One bounded architecture repair, two corpus corrections, validation, one clean Medium run.

# The turn path — an inventory of everything that runs unconditionally, 10 September 2026 — report only

Asked for by Lord Armand after the recall finding: not defects, design shape — which work on a turn is universal because it must be, and which is universal because nobody asked. Figures are from persisted rows and logs of real and scratch turns on `opus-5-medium` with persona v1.4; nothing here is implemented.

## 1. The turn path, in order, with what each step costs on a turn that does not need it

"Does not need it" is measured on the two live turns of 10 September ("Hello, Val!" and a greeting correction) and the ordinary prompts of the packet runs. Wall-clock for all local steps together is the end-to-end time minus the model calls: **0.13 s and 0.10 s** on the two live turns.

| # | Mechanism | Runs | Series or parallel | Cost on a turn that does not need it | Model call? | Could it be conditional, and what would break |
|---|---|---|---|---|---|---|
| 1 | Restricted-content check on the typed text | every turn | series, first | local, ~ms; 0 tokens | no | Must be universal: it is the egress guard (invariant: content is classified before it leaves the house). Nothing to gate. |
| 2 | Scope resolution and conversation open or resume | every turn | series | local, ~ms | no | Universal by definition. |
| 3 | Persist the user message | every turn | series | local, ~ms | no | Universal: history before any provider (WP-0.7 ordering). |
| 4 | **Consequential classifier** (Haiku, schema-constrained) | every turn | **series, before the partner call** | **1.3–1.6 s; ~720 tokens in, ~25 out; US$0.0008–0.0009** | **yes** | The verdict is needed only to choose the path (ordinary versus strip → blind → reconcile). A **deterministic pre-filter** can decide `no_choice_present` for a bounded class of turns without a model call (§3). What would break if it were wrong: a consequential turn answered on the ordinary path with the preference in view — the failure §4.1 exists to prevent — so the pre-filter may declare only what it can decide by shape, and everything else goes to the classifier. Note the classifier is itself non-deterministic (the identical O11 prompt drew different verdicts across runs). |
| 5 | Strip (Sonnet) | consequential turns only | series | not on ordinary turns; on consequential ones ~2–4 s, ~1,150 tokens in / 80 out, ~US$0.003–0.006, and it returns `no_preference` on some (O9, O11: the turn then collapses to one call) | yes | Already conditional on the verdict. A deterministic "nothing to strip" pre-check would have to be certain no first-person stance is present, which is semantic; the strip *is* the gate. Justified as is. |
| 6 | Blind position (partner route, pinned) | consequential turns with a separable preference | series | not on ordinary turns; ~10–20 s and US$0.07–0.16 when it runs | yes | Already conditional; structural, must stay (§4.1 ordering). |
| 7 | Same-conversation history load and contiguous-tail budget | every turn | series (local) | local, ~ms; the *tokens* it sends are the conversation itself, 0 on a first turn, up to ~88K provider tokens at the cap (US$0.41) | no | Universal by design (the thread is the context). Its cost is the history-budget question, not a gating one. |
| 8 | **Recall — full-text search across other conversations in scope** | **every turn** | series (local query, ~ms) | **the excerpts it admits: 0–16K estimated tokens (up to ~21K provider); on "Hello, Val!" six excerpts ≈ 6,000 tokens ≈ US$0.03; on the correction one excerpt ≈ 14,500 tokens ≈ US$0.07 — a third and two thirds of those turns' partner cost; latency effect is indirect (larger input) and small** | no (the search); yes in effect (the tokens ride the partner call) | **Universal because nobody asked.** A deterministic gate can skip it when every referent is inside the thread (§2). What breaks if the gate is wrong: a turn that needed outside material is answered without it — she has less to see, not less capability — and the envelope already carries a distinct `not_run` state so she can say so. Failing toward recall bounds the error. |
| 9 | Record-state envelope | every partner call | series (local) | ~250–350 tokens ≈ US$0.0013–0.0018; ~0 latency | no | Ruled universal on 9 September and justified: absence must be stated. One design interaction to record: placed *first*, its changing counts invalidate any history-prefix cache (measured: doubles the cost of the caching arrangement); placed after the history it would not. |
| 10 | Persona, whole, in `system`, cached 1 h | every partner and blind call | series (local load; the cache is the provider's) | read US$0.0029 per call (5,746 tokens at $0.50/M); **cold write US$0.0575 whenever the hour lapses**; latency effect none measurable | no | Universal by ruling (loaded whole, never trimmed). The recurring cost is the lapse: **live store since caching went live on 8 September: one cold write (10 September, first turn of the day) and one hit**; the scratch runs paid 14 cold writes in the exported stores (each run resets its store; a C prompt writes a second, schema-bearing entry) ≈ US$1.5 of the month, plus the unexported runs and the measurement. A scattered day of six bursts more than an hour apart would pay about US$0.35 in persona writes; hourly keep-warm reads would cost about the same and are provider calls with no user behind them. |
| 11 | Budget preflight and reservation | every provider call | series (local) | local, ~ms | no | Universal by invariant (ceilings enforced before a call). |
| 12 | Partner response call | every turn | series, last | **the floor: 5–9 s and US$0.05–0.12 on a short ordinary turn**, dominated by generation (thinking plus visible output) | yes | This is the work. |
| 13 | Settle and persist (message, `model_calls`, cache usage, classification row, budget settlement) | every turn | series (local) | local, ~ms | no | Universal by invariant (Layer 0 capture). |

**Sum on an ordinary turn that needs nothing outside the thread:** the classifier (1.3–1.6 s, ~US$0.001) and recall (US$0.03–0.07 when it admits material; 0 s) are the only unconditional costs that are neither the work itself nor invariant-bound. Everything else local is ~0.1 s in total. The persona write is per lapse, not per turn.

## 2. Should retrieval have run at all — and is a gate the rejected carve-out?

**The distinction holds.** The rejected carve-out was a classifier deciding when Val may be *less capable* (a cheaper model or lower effort on turns judged trivial). A deterministic recall gate changes *what she can see*, not how she reasons, and it does so on turns whose referents are inside the thread — where there is nothing outside to see. Three things keep it on the right side of that line: it is mechanical, with no model judging the turn; it fails toward recalling; and the record-state envelope already has a `not_run` state, so a skipped retrieval is stated to her as a fact and never mistaken for "nothing exists". The WP-0.7 acceptance ("Val recalls prior context within a project") is unaffected on any turn where recall runs; the gate must never skip a turn that might reach outside the thread.

**The smallest deterministic version.** Recall is skipped only when *every* condition holds; otherwise it runs:

1. The message is short — at most about fifteen words — and contains no digits and no quotation marks (a quoted string or a number is a referent by shape).
2. It contains no capitalised token other than sentence-initial words, "I", "Val", "Lord", "my lord".
3. It contains none of a fixed back-reference lexicon: *earlier, before, last, yesterday, remember, recall, discussed, agreed, decided, settled, said, mentioned, previously, again, the other, that one* (and the obvious inflections).
4. It contains no question mark. (A question may reach outside; fail toward recall.)

That admits greetings, acknowledgements, reactions, and form instructions ("shorten that", "try again", "one word", "go on") to the no-recall path, and sends everything else to recall. Applied to the two live turns: **"Hello, Val!" is skipped** (three words, no back-reference, "Val" allowed); **the greeting correction is not** — it carries quotation marks, digits and a sentence ending in a request — so the smallest gate would not have saved the seven-cent excerpt. A second tier catches that case precisely and still deterministically: a quoted string that occurs verbatim in *this conversation's own last Val message* is an in-thread referent ("You greeted me with 'Good evening'"), and a message whose only quoted strings and capitalised tokens are in-thread by that test is skipped. Both tiers are pure string rules over the message and the stored thread; neither consults a model or the recall index.

What the gate does not do: it does not fix ranking or the admit-whole rule on turns where recall runs. Those remain the separate questions already reported (lexical ranking; the top candidate admitted whole regardless of size).

## 3. The classifier — mechanically decidable exclusions and the latency floor

**Mechanically decidable, narrowly.** Of the six hard exclusions, five are semantic (retrieval, a fact stated, execution of a decided task, status queries, logistics) and are not safely decidable by shape. The sixth, `no_choice_present`, has a decidable *subset*: a short message (about fifteen words) with **no alternative-marker** — none of *or, either, whether, versus, vs, option, choose, pick, decide, decision, commit, go with, should we, do we, which, instead* — and no question mark, contains no choice among alternatives by construction. Greetings, acknowledgements, reactions, corrections of fact, and form instructions fall in it; "Stop. One word: harbour or workshop?" does not (it has *or* and a question mark), which is right — that prompt is a genuine choice and drew inconsistent verdicts precisely because it is one. A pre-filter verdict would be recorded in `classifications` as an established `not_consequential` with `hard_exclusion = no_choice_present` and a marker that the rule, not the model, decided it, so the fifty-exchange audit can see both kinds. This is compatible with the 3 September rule that *unknown* classification is never ordinary: a rule-established verdict is not unknown; only the model's silence is.

**The latency floor.** On an ordinary turn the verdict is needed only to choose the path; the partner call does not otherwise depend on it. Three shapes:

- **Parallelise the classifier with the local assembly** (history, recall, envelope): saves ~0.1 s. Not worth a design change.
- **Start the ordinary partner call in parallel with the classifier and discard it if the verdict is consequential or unestablished:** saves the full 1.3–1.6 s on every ordinary turn; costs the discarded call's full price on every consequential turn (US$0.05–0.40, a minority — 8 of 36 packet prompts; 2 of 4 live classifications this month) and writes a `model_calls` row for a response that is never persisted as a message. Ordering stays structural (the discarded call is not her position and nothing of it is recorded but its cost), but it is a spend-for-speed trade and a new evidence shape, which makes it a ruling rather than an engineering choice.
- **The deterministic pre-filter** removes the classifier entirely from the turns it covers: no cost, no latency, no waste, no new evidence shape. Where it does not apply, the classifier runs in series as now.

## 4. Layer 2 and after — does the principle govern tool decisions?

It governs the **cost** dimension exactly: tool definitions sent with every request are the recall problem again (every turn pays tokens for schemas it will not use; a model-driven "does this need a tool" check per turn is a classifier on every turn). The same shape applies — offer tools only when a deterministic signal says the turn may need them (an explicit request, a session with a declared tool scope, a referent outside the thread), and fail toward offering when ambiguous. It does **not** answer the **authority** dimension, which already has its own rules and must keep them: discovery is never authorisation, capability is not permission, and whether a tool may be *used* on a turn is never a cost question. So: the principle for whether tools are *offered*; the existing invariants for whether they may be *used*. That part needs its own answer when Layer 2 is designed, and the principle should say so rather than appear to cover it.

## 5. The principle

*Any mechanism that runs on every turn must justify running on every turn. Prefer a deterministic gate over a model call. Prefer conditional over universal. Fail toward doing the work when the turn is genuinely ambiguous.*

**It is the right principle**, and this inventory is its first application: of thirteen mechanisms, nine are universal by invariant or by definition, two are universal by ruling and justified (persona, envelope), and two are universal because nobody asked (recall, the classifier on turns a rule could settle).

**Conflicts:** none with governing text, provided two readings are recorded with it. (a) A gate decides *whether work runs*, never *how capable Val is*; the 8 September rule that no classifier decides how hard she thinks stands untouched. (b) A gate's decision is a positive rule with a recorded outcome, never an absence: the 3 September rule that unknown classification is never ordinary stands, and a gated recall is stated as `not_run`, never as zero. It also does not license trimming anything ruled whole (the persona) or reordering anything ruled structural (classify → strip → blind → respond on consequential turns).

**Where it should live:** `01-architecture.md` §5.5, beside the cost doctrine, as the rule that shapes the turn path — with a cross-reference from CLAUDE.md's standing exclusions as a fourth failure to watch for ("universal because nobody asked"), since that is where an implementer meets it. The tool-offering clause belongs with it, marked as governing offering only, with authority left to the invariants.

## 6. The recall query itself — the OR rewrite's candidate set, tested against the two captured turns (added 10 September 2026, report only)

Lord Armand's reading of `memory.py` is confirmed: `plainto_tsquery`'s `&` operators are rewritten to `|` before the cast, so the match set is *any message containing any one of the query's lexemes*, `ts_rank` then sorts that set with its default normalisation (no division by length), and the 7 September admission rule hands the top of it through whole. Every figure below is a read-only query on the live store's own `tsvector` data over the recall scope of the two turns (the no-project pool minus the current conversation: 48 messages). No provider was called.

**The candidate sets as they were.** "Hello, Val!" → lexemes `hello`, `val`; the OR query matched 9 messages (AND would have matched 3). The correction → eleven lexemes; the OR query matched **36 of 52** (AND: 1). One correction of Lord Armand's inference: `val` is not in nearly every message — 7 of 48 in this scope, because her own replies rarely say "Val" — the haystack came from `good`, `correct`, `immedi`, `refer`, `even`, each in 7–17 of 48 messages; a 39,000-character screenplay contains four of the nine substantive lexemes (`morn`, `good`, `even`, `immedi`) and no short message can compete with that under unnormalised rank.

**1. A term-quality floor.** Implementable deterministically against the existing data: document frequency per lexeme is one indexed count on `ix_messages_content_fts` (about 10 ms per term on this store; 12 terms in 121 ms), computed at query time from the scope, no table, no embeddings, no provider. Tested on the captured turns:

| Floor (term must appear in ≤ this share of scope messages) | "Hello, Val!" | The correction |
|---|---|---|
| 10% | keeps `hello` only → 2 candidates, 30 and 36 characters (was 6, including 9,581 and 4,360) | keeps `10`, `morn`, `greet` → 6 candidates; **the screenplay still first** (a screenplay has MORNING in its slug lines; `morn` is 4 of 48 here because the current conversation, which says "morning" three times, is excluded from its own recall) |
| 15% | keeps both → the same 6 as live | keeps five terms → screenplay first |
| 20% | same | same |

So the floor is **surgical on the greeting** (it turns six excerpts into two tiny ones) and **does not remove the screenplay on the correction**, because the one selective word the correction shares with it is exactly the word a screenplay is full of. Thresholds are also fragile at this corpus size — one message is 2% of the scope — and a floor needs a rule for the case of no selective term (return zero, or fall back to the rarest terms), which is a design choice.

**2. Conjunction scaled to query length.** Worse than the floor on the observed failure and redundant with it on the greeting. On the correction, the screenplay matches 4 of the 9 substantive lexemes, so requiring 2 (21 candidates) or 3 (8 candidates) keeps it and ranks it near the top; only 5 of 9 excludes it, leaving one candidate — the 20,923-character project overview — which is AND by another name. Conjunction counts hits, and long documents accumulate hits: it selects *for* the failure mode.

**A third lever the code reading exposes: rank normalisation.** `ts_rank(vector, query)` is called without a normalisation flag, so rank is not divided by document length and the longest document wins any tie on common words. Tested: with normalisation 2 (divide by length) the correction's top six become the shortest messages in the scope (30–506 characters) and the screenplay drops out; with 1 (log length) the screenplay stays first. Normalisation is a one-integer, zero-cost, deterministic change that stops long documents dominating, but on its own it merely swaps a 39K-character wrong answer for a 30-character wrong answer: on both captured turns **no candidate in the store was relevant**, because the referents were inside the thread. It belongs with the floor, not instead of the gate.

**3. The gate.** The only lever that gives the *right* answer on both captured turns: "Hello, Val!" is skipped by the first tier (short, no digits, no quotes, no back-reference, no question), and the correction is skipped by the second tier (its quoted strings "Good evening" and "this morning" occur verbatim in her own last message, so its referents are in-thread). Zero cost, zero latency, no ranking involved, and the envelope states `not_run`.

**How the three interact, read from the code and the data.** The gate is the biggest win and the only one that addresses the prior question; on the turns it skips, nothing else runs. On turns where recall *should* run, the floor is the most surgical improvement to the candidate set, and rank normalisation is its natural companion (the floor shrinks the haystack; normalisation stops the longest straw winning); together they would have reduced the greeting to two tiny excerpts and, on a genuine outside-thread query, stop a long document from being admitted merely for containing common words. The conjunction rule is unnecessary if the floor is adopted and harmful without it. Two things none of the three touch: the admit-whole rule of 7 September, which is what turned one lexical hit into 14,500 tokens (a per-message ceiling is a change to that ruling, not an implementation detail), and the OR rewrite itself, which stays as ruled. Lord Armand's instinct holds on all three counts.

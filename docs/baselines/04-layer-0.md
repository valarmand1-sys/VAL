# 04 — Layer 0

**Status:** Governing on the first work packages.
**Owns:** Layer 0 scope, schema, acceptance criteria, the gate.
**Does not own:** why any of it is shaped this way — see `01-architecture.md`.

**Estimate:** 2–3 weeks. **Machine:** the existing MacBook Pro. **Credentials:** model providers and backup storage only.

---

## 1. What Layer 0 is for

*Val exists, remembers, and is useful across projects.*

Layer 0 delivers a text conversation with a persistent, project-aware assistant who has her character from the first exchange. That is the visible part, and it is the smaller part.

**The value of Layer 0 is almost entirely in the capture obligations being correct from the first commit.** Execution history, deliberation records, and per-call cost attribution feed machinery that does not exist until Layers 3 and 5. If they are wrong or absent now, that machinery has nothing to consume and the gap cannot be filled later. Everything else in this layer can be rewritten in an afternoon; the captured record cannot be recreated at any price.

**Scope discipline is the second requirement.** Layer 0 staying small is deliberate. The following are explicitly out, and an implementation that adds them has failed the layer regardless of how well they work: Temporal, transactional outbox, audit chain, approval flow, tools of any kind, MCP, agents or Roles, voice, avatar, local inference, the graduated budget thresholds, the reserve, the cost dashboard, lesson distillation, success models.

### 1.1 Data classification at Layer 0

Every Layer 0 conversation carries real project content. For the first project that is unreleased creative IP — **Protected** classification — and it goes to cloud providers. Invariant 17 applies from the first exchange, before any tool exists.

The full per-content classification machinery is not built here. The obligation is met structurally instead:

**Only providers declared eligible for Protected content may be configured at Layer 0.**

With no ineligible route configured, eligibility is satisfied by construction rather than by a check on every call. There is no route to which Protected content could be misdirected, because no such route exists in the system. This is cheaper than per-call classification and stricter in effect.

Two consequences follow, and both are binding:

- **Cost never selects a route at Layer 0.** With every configured route Protected-eligible, choosing among them on cost is permissible; choosing a route *because* it is cheap, from a set that includes ineligible options, is not. The set contains no ineligible options.
- **Restricted content cannot be processed at Layer 0 at all.** Credentials, financial detail, and third-party personal data route to local inference only (`01-architecture.md` §5.4), and local inference does not exist until Layer 1. Val states plainly that she cannot handle it yet. She does not route it anywhere, and she does not reclassify it downward to make it routable.

**The structural guarantee covers routing, not content.** Every configured route being Protected-eligible means Protected content cannot be *misdirected*; it says nothing about a user message that happens to contain an API key. That gap is closed by a small deterministic local preflight in `packages/policy` — see WP-0.4's criteria. The preflight reads content and blocks; it never reclassifies downward, and it never asks the receiving model whether it should receive it.

Per-content classification arrives at Layer 2, when tools begin pulling in content of mixed classification and the structural guarantee no longer holds.

---

## 2. Schema

One migration set, Alembic, from the first commit. No manual DDL at any point.

### 2.1 Core

**`projects`** — `id`, `name`, `slug`, `description`, `status`, `created_at`, `updated_at`, `archived_at`

**`conversations`** — `id`, `project_id` (nullable — "no project" is a real, explicit state, not a null accident), `title`, `started_at`, `last_message_at`, `archived_at`

> *12 September 2026:* `project_id` is the immutable **origin** scope; the effective scope after an explicit move is derived from `conversation_scope_transitions` (the §2.1 amendment of that date, item 8).

> **Amendment — 31 August 2026, Lord Armand. `archived_at` is presentation scoping and carries no evidentiary meaning.** Nullable, lifecycle-class (mutable, like `conversations.title`), on `projects` and `conversations` only. Set means exactly one thing: *hidden from the interface's default listings*. It means nothing else. **It must never be read as superseded, retracted, mistaken, or not real** — the first rows ever marked (migration `0012`: the seven demonstration projects and eighteen demonstration conversations of 15–19 August 2026) are all cited Layer 0 gate evidence, real conversations with real providers, preserved whole. Archiving changes no behavior outside listings: an archived conversation still resumes, an archived project still resolves and still scopes recall, and every capture and evidence table is untouched. Deletion remains impossible (§2.3) — archiving exists precisely because it is not deletion.
>
> **Standing rule, same date: demonstration fixtures never enter the live store again.** The August fixtures exist because the early acceptance runs deliberately ran live; that era is closed. Anything future that needs seeded data — with or without live providers — runs in a scratch database. The one exception is the Layer 0 gate's own single-session demonstration, which is real use through the interface and belongs in the live store by definition.

**`messages`** — `id`, `conversation_id`, `role` (`user` | `val` | `system`), `content`, `created_at`, `sequence`

> **Requirement — 31 August 2026, Lord Armand. Revision and retraction of his own messages. Recorded against the core loop; no work before Lord Armand authorizes it after the Layer 0 gate.**
>
> Lord Armand must be able to correct and retract messages he has sent — typos, a badly worded question, something sent to the wrong conversation. Today a mistake is permanent and visible forever, and that is a defect in daily use.
>
> **The bound he set with the requirement: the append-only record is not weakened.** The honest version is *correction and retraction, never erasure*: the original stands, the revision stands, and which came later is visible. A retracted message stops cluttering the live conversation and is not treated as live intent, but remains in the record. This is the shape the record already uses everywhere — the persona's edit-is-a-new-revision, `idea_state_changes`' append-only lineage, `model_calls_accounted`'s supersede-by-view — applied to `messages`.
>
> **Design constraints that bind any implementation:**
>
> - `messages` rows stay frozen (`0009`) and undeletable (§2.3). Revision and retraction are *new facts appended*, never edits to the original.
> - Retraction authority is per-author: this mechanism touches `role = 'user'` rows only. Nobody retracts another speaker's words — Val's answers are hers.
> - Evidence anchors never dangle: `model_calls`, `execution_events`, `deliberations`, and `blind_positions` reference specific message ids, and a retraction must leave every anchor resolving to exactly what was said when the anchored event happened.
> - Money already spent answering the original stays spent and stays attributed. A retraction rewrites no cost.
>
> **Open questions, his to decide when this is taken up** (options and recommendations were given at recording time): what recall does with a retracted message; what happens to an answer whose question is then revised; and how the sunk cost of a retracted exchange is shown.

> **Amendment — 12 September 2026, Lord Armand. Conversation and message management, ruled on the diagnostic (`docs/reviews/VAL_Conversation_Management_Diagnostic.md`, accepted as the pre-implementation record).** The open questions above are decided. Every model below is append-only; no `messages` row and no `conversations.project_id` is ever updated; hard delete stays impossible; evidence anchors never move.
>
> 1. **Message revision and retraction — `message_revisions`.** One append-only, undeletable sidecar: `message_id` (a **user** message only — Val's words are never revised or retracted), per-message `revision_number`, **`after_sequence` — the conversation's highest `messages.sequence` read while holding the conversation row lock**, `kind` (`revision` | `retraction`), `content` (present exactly for a revision), `authored_by`, and an optional `note` that is never required. A revision consumes no message sequence. **As-of rule:** the turn whose user message has sequence *s* sees, for every earlier message, the latest fact with `after_sequence < s` — never a timestamp comparison — so a revision can never alter the reconstructed input of any provider call, classification, strip, blind position, deliberation or event that happened before it. Current text is the latest fact's; a revision after a retraction reinstates. One authoritative derivation (`messages_current`) states the rule once without mutating the record.
> 2. **Downstream answers — Option 1.** When A (answered by Val's B) is revised to A2: A2 is the current wording in A's position; A stays available as the original; B stays visible as Val's historical answer and is marked as answering the earlier wording; later messages stay where they are; the record-state envelope tells Val deterministically that the earlier position was corrected after it was answered. **An edit never triggers a provider call and implies no regeneration.** No combined "correct and re-ask" in v1.
> 3. **Retraction.** A retracted user message A and its immediate Val answer B stay permanently in the record, marked withdrawn; both leave ordinary current-thread assembly for later turns and are excluded **in the query** from automatic recall and from House Recall; downstream messages stay; the envelope states that an earlier exchange was withdrawn without injecting its contents; classifications, strip evidence, blind positions, deliberations, judgments, execution events and cost stay unchanged and attached to the original exchange. The interface shows the withdrawn message collapsed and inspectable, with B beneath it marked as answering a withdrawn message. **In v1 Val does not receive withdrawn or superseded wording through ordinary recall or House Recall, even when asked;** a gated historical-inspection capability is a later decision.
> 4. **Deliberated messages — §9.1 Option 1.** A user message that anchors an `ordering = enforced` `blind_positions` row or any `deliberations` row **may not be revised**; the writer refuses deterministically, and the interface says the message is part of a recorded decision exchange and cannot be rewritten. It may be **retracted**, which withdraws the exchange from live intent and ordinary recall while preserving the message, the strip evidence, the blind position, the deliberation, Val's answer, judgments and cost exactly. **A later retraction does not retroactively invalidate genuine Layer 0 gate evidence or a genuine hand-labelled classification that was valid when produced;** retraction is not an evidence-delete mechanism, and an event later found never to have been eligible evidence is handled under the evidence rules, not by retracting the conversation.
> 5. **Rename** uses the existing mutable `title`; no title history in v1, and the minor imprecision that a later reconstruction may show the current title beside an old excerpt is accepted.
> 6. **Archive / Unarchive** use `archived_at` with its 31 August 2026 meaning exactly: hidden from default sidebar listings, nothing else.
> 7. **Remove / Reinstate — `conversation_removals`.** A distinct append-only withdrawal state for a whole conversation (`removed` | `reinstated`, per-conversation `event_number` under the conversation lock). A removed conversation is completely preserved, excluded from automatic recall and from House Recall, **cannot be resumed for new turns**, retains every message, evidence row, judgment and cost, and is reinstated by another appended fact. The ordinary label is **Remove**, never Delete. True byte erasure is a separate privacy/legal operation outside this scope, and the hard-delete protections are not weakened.
> 8. **Moving a conversation — Option B, scope transitions; the forward-only doctrine amended for one narrow case.** `conversations.project_id` remains the immutable **origin** scope (`0008` stands). An append-only **`conversation_scope_transitions`** row — prior effective scope, new effective scope (explicit no-project included), `after_sequence` under the conversation lock, per-conversation `transition_number`, `authored_by` — establishes the **effective scope** for subsequent turns. An explicit Move to project / Move out of project by Lord Armand may change the effective scope of an existing conversation from that point forward; it never rewrites the scope under which earlier messages or calls occurred. After a transition: the sidebar lists the conversation under its current effective scope; resuming uses it; new user messages, classifications, deliberations, execution events and model calls are attributed to it; earlier calls keep their attribution and earlier messages keep the effective scope in force when written; **automatic recall scopes every message by its own effective scope**, so a move into Project B never brings unrelated Project A material from elsewhere into B; House Recall remains house-wide with accurate per-message source provenance. The moved conversation's own retained history may contain pre-transition material — **authorised, not leakage**, because continuity of the thread is the requested operation. Moving to explicit no-project works the same way: no automatic recall for future turns, the thread's own history retained. Scope stated inside a turn still starts a new conversation (WP-0.6, unchanged); no filing-only system and no linked continuation in v1.

**`personas`** — `id`, `version`, `semantic_version`, `content`, `source_sha256`, `source_path`, `created_at`, `is_active`, `activated_at`, `authored_by`. Versioned from the first load. Editing the persona creates a version; it never mutates a row.

> **Clarification — 17 August 2026, Lord Armand. Recorded before WP-0.5 begins, not implemented by it.**
>
> **`personas.version` is a persistence revision, not the persona's semantic version.** It is a monotonically increasing, immutable record number — `1`, `2`, `3`, … — and it counts rows, not authorship. The authored document keeps its own label: **v1.1, v1.2, v2.0**. The two are different scales measuring different things, and the row seeded from the current persona document therefore carries persistence revision **`1`** whatever semantic label that document bears.
>
> **The persistence revision must never be presented as the persona's semantic version.** An interface showing "Persona v1" over a row seeded from v1.2 is displaying a state the record does not support (invariant 29), and the mistake is invisible until someone asks which persona was live during a conversation.
>
> **The semantic label was not storable when this was written.** §2.1's column list had no field for it, and the clarification proposed one without adding it. **Implemented by WP-0.5 on 17 August 2026** — see the amendment below.
>
> **Immutability and activation, stated exactly**, because the current wording admits two readings and only one is intended:
>
> | Property | Rule |
> |---|---|
> | Authored content | **Immutable once stored.** `content` is never updated on an existing row. |
> | Record identity | **Immutable.** `id` and `version` never change. |
> | Editing the persona | **Creates a new row** at the next revision. It never rewrites the prior one. |
> | Prior content | **Never overwritten**, and never deleted (§2.3). |
> | `is_active` | **Lifecycle state, and it may change.** It is selection, not authorship. |
> | Activating a revision | **One transaction** that deactivates the former active row and activates the new one. |
> | How many may be active | **Exactly one**, enforced by a unique partial index rather than by convention. |
> | Activation and history | Changing `is_active` **never touches authored content.** Which persona is live is a different fact from what any persona says. |
>
> The distinction is the whole point: the persona's *content* is an authored artifact and is append-only; the persona's *activation* is operational state and moves. Collapsing them gives either a persona that cannot be switched or a history that gets edited to make the current state convenient — and the second is what invariant 14 exists to prevent.

> **Amendment — 17 August 2026, Lord Armand, authorising WP-0.5.** Four columns added to `personas`, and one nullability corrected. Executive decision 2 of that date: *WP-0.5 shall store the authored Persona semantic version explicitly and separately from the integer persistence revision.*
>
> | Column | Why |
> |---|---|
> | `semantic_version` | The authored label — `1.2`, canonical and without its `v`. **NOT NULL**, where the clarification above proposed nullable: a row that cannot say which authored version it holds is exactly the ambiguity the column exists to remove, and every row is written by a seeder that knows the answer. Narrowing a proposal is permitted; widening one is not. |
> | `source_sha256` | SHA-256 of the exact bytes of the document this row was seeded from. |
> | `source_path` | Which document, **repository-relative**. An absolute path is a fact about one machine, and a record that made one authoritative would be unverifiable anywhere else. |
> | `created_at` | When the row was written — a fact `activated_at` cannot carry once activation starts moving. |
> | `activated_at` → **nullable** | NULL means *this revision has never been active*. A revision created and not yet activated previously had to carry an activation instant for an event that had not happened. |
>
> **Immutability is enforced by the database.** A `BEFORE UPDATE` trigger refuses any change to `version`, `semantic_version`, `content`, `source_sha256`, `source_path`, `created_at`, or `authored_by`. `is_active` and `activated_at` sit deliberately outside it. Service code can be bypassed by the next caller; a trigger cannot, and `personas` is the one table whose historical content the whole of Layer 5 will later read back.
>
> **The canonical reading rule, stated once because WP-0.5's two checks are only independent if it is single.** The governing document is read as raw bytes; its digest is the digest of those bytes; its content is those bytes decoded as strict UTF-8 and stored verbatim. **No normalisation is applied** — none to newlines, trailing whitespace, or Unicode form. No normalisation *is* the normalisation, and it is the strongest available choice: every alternative requires trusting that the same transformation happened in both places, and this one has nothing to get wrong.
>
> **The authored label is parsed deterministically from the document's H1 and from nowhere else.** It is never inferred and never asked of a model, which would make the record depend on the thing it exists to govern. Migration: `0005_persona_provenance`.

### 2.2 Capture

These three tables are the point of the layer.

**`model_calls`** — per-call cost attribution

| Column | Note |
|---|---|
| `id`, `created_at` | |
| `model_config_id` | The configuration, not a bare model string |
| `provider`, `model_identifier` | Denormalised deliberately — a retired config must still resolve historically |
| `tokens_in`, `tokens_out` | **Nullable since the 17 August 2026 amendment** — NULL exactly when `cost_certainty = 'unknown'` |
| `cost` | Computed at call time from the config's rates. Never recomputed later. **Nullable on the same terms.** |
| `cost_certainty` | `known` \| `unknown`. **Nullable** only on rows written before this amendment. |
| `project_id` | Nullable, matching `conversations` |
| `task_type` | Enumerated. Layer 0 values: `conversation`, `classification`, `strip`, `blind_position`, `title` |
| `conversation_id`, `message_id` | Nullable |
| `persona_id` | **WP-0.5, 17 August 2026.** Which persona revision was assembled into this call's context. Nullable: rows written before a persona existed, and paths that legitimately assemble none, are not calls to attribute. **19 August 2026:** `blind_position` calls carry the persona and therefore attribute it — once a persona is assembled, a NULL here would mean "assembled and failed to attribute," which is a false record, not a missing feature. |
| `latency_ms`, `provider_request_id` | |
| `status` | `ok` \| `error` \| `refused` |

`cost` is stored, not derived. Provider pricing changes, and a historical record that silently re-prices itself is not a record.

> **Amendment — 8 September 2026, Lord Armand. Prompt caching, and its evidence.** Caching is enabled on the partner route's stable prefix (WP-0.4 amendment of this date). `model_calls` keeps its shape: `tokens_in` is everything the provider processed as input — uncached plus cached — and `cost` is the total billed at call time from the provider's own usage figures, never recomputed. The split is evidence in its own right and lives beside the call:
>
> **`model_call_cache_usage`** — at most one row per `model_calls` row, present exactly when caching was requested on that call and the provider reported usage. Columns: `id`, `created_at`, `model_call_id` (unique), `requested_ttl` (`5m` | `1h`), `uncached_input_tokens`, `cache_write_5m_tokens`, `cache_write_1h_tokens`, `cache_read_tokens`, `outcome` (`hit` | `created` | `hit_and_created` | `not_cached`), and the four billed components `cost_uncached`, `cost_cache_write`, `cost_cache_read`, `cost_output` at the configuration's verified cache rates. Append-only, frozen by the standing guards (`0015`). A hit, a miss, a creation, the cached and uncached counts, and the actual bill are therefore all distinguishable from the record, as the ruling requires.

> **Amendment — 17 August 2026, Lord Armand, after external review.** A provider attempt has three accounting outcomes and only two of them are rows:
>
> | Outcome | Provider reached | Recorded |
> |---|---|---|
> | **NOT_SENT** | No | **No row at all.** Cost is definitively zero, and it was not a model call. |
> | **SENT_COST_KNOWN** | Yes | Real figures, `cost_certainty = 'known'` |
> | **SENT_COST_UNKNOWN** | Yes, or possibly | `cost_certainty = 'unknown'`, figures **NULL** |
>
> The implementation had been writing `tokens_in = 0, tokens_out = 0, cost = 0` for every failure, including failures that occurred after the request reached the provider. That is not an unknown recorded as unknown; it is a figure known to be false recorded as a fact, and it flowed into the month-to-date total the ceiling was enforced against. **A call that reached the provider consumed its input tokens whatever happened to the response.**
>
> Two check constraints make the false zero unwritable rather than merely discouraged: `known` must carry figures, and `unknown` must carry none. Rows written before this amendment keep every figure they hold and carry a NULL certainty, meaning *written before the distinction existed* — the 0002 precedent, NULL rather than a neutral value, because guessing which state an old row deserves would be inventing evidence.
>
> **The rule about errored calls and spend, stated truthfully.** A refusal and an error that reports usage count at their real cost. An error that reports no usage cannot count at its real cost because nobody knows it — so it counts against the ceiling at its **reserved maximum** (§2.5) while its `model_calls` row records the cost as unknown. The ledger is conservative about what may be gone; the call record is honest about what is known. They differ on purpose.

> **Clarification — 19 August 2026, Lord Armand. SENT_COST_UNKNOWN versus process-death indeterminacy.**
>
> SENT_COST_UNKNOWN applies when Val remains alive and can durably record an
> attempted provider operation whose outcome or cost is unknown — a timeout or
> provider error. Process-death indeterminacy before durable `model_calls`
> evidence exists is governed instead by §2.5: the surviving
> `budget_reservations` row becomes `expired`, represents that transmission may
> or may not have occurred, remains charged at its authorized maximum, and does
> not cause Val to fabricate a `model_calls` row on restart. If independent
> provider-side evidence later establishes that a call occurred, that evidence
> may be appended truthfully; it is never inferred from the expired reservation
> alone.
>
> *Recorded because the two sections overlapped: both describe an attempt whose
> outcome is unknown, and without this line the SENT_COST_UNKNOWN row could be
> misread as required in the crash case, where no process survives to write it.*

> **Second amendment — 17 August 2026, Lord Armand. The five superseded rows, and `model_calls_accounted`.**
>
> The amendment above stopped the false zero being *written*. It did nothing about the five already in the store: rows from 15 August 2026 carrying `tokens_in = 0, tokens_out = 0, cost = 0.000000, status = 'error'` and a NULL certainty. **They are preserved exactly as written** — invariant 14 forbids editing history to make the present tidy — but preserving them is not the same as leaving them readable. As they stand, `sum(cost)` counts them as five confirmed free calls and nothing marks them as anything else.
>
> **The rule that identifies them is exact, not heuristic.** The superseded implementation wrote `0, 0, 0` on *every* `GatewayError`, unconditionally, and real usage on every success and refusal:
>
> | Row | Means |
> |---|---|
> | `cost_certainty IS NULL` and `status = 'error'` | Cost **unknown**. The zero was fabricated. |
> | `cost_certainty IS NULL` and `status <> 'error'` | Cost **known**. Real usage was recorded. |
> | `cost_certainty IS NOT NULL` | Says what it is. |
>
> Two mechanisms apply it, and **neither adds a table or a row**:
>
> - **A check constraint bounds the legacy set permanently.** No row created from 17 August 2026 onward may leave `cost_certainty` unstated. A NULL therefore means *written before the distinction existed* and can never come to mean anything else, so the rule cannot silently widen to rows it was not written for.
> - **The view `model_calls_accounted`** projects every column of `model_calls` plus `effective_cost_certainty` (never null), `accounted_cost` (NULL when the cost is not known), and `accounting_note` (the explanation, in words, on superseded rows only). **Every query that touches money reads the view.** SQL, Python, and Layer 5 distillation therefore share one interpretation, because there is only one.
>
> **This is the one view §2 names**, and it is named for the same reason §2 names tables: so that nothing arrives unnamed. It holds no state of its own and cannot drift from the base table, because it *is* the base table read through a rule.
>
> **Both halves stay reconstructable, which is the requirement this satisfies.** `SELECT * FROM model_calls` returns the original evidence, `0.000000` still there. `SELECT * FROM model_calls_accounted` returns what it means. The correction itself is the migration — dated, attributable, in git, append-only in the way migrations are. Neither half is derived from the other's absence, and no row was mutated or deleted to produce either.

**`execution_events`** — every acceptance, rejection, revision, and correction

| Column | Note |
|---|---|
| `id`, `created_at` | |
| `project_id`, `conversation_id`, `message_id` | |
| `event_type` | `accepted` \| `rejected` \| `revision_requested` \| `corrected`. **Nullable since the 15 August 2026 amendment** — a reaction with no event is a real record. At least one of `event_type` and `reaction` must be present. |
| `subject` | What was accepted or rejected — free text at Layer 0 |
| `reason` | **The reason, in Lord Armand's words where he gave one.** Nullable, but a null reason is a defect to be surfaced, not a normal state. |
| `reason_source` | `stated` \| `inferred` \| `absent` |
| `reaction` | `negative` \| `neutral` \| `interested` \| `enthusiastic` \| `strongly_enthusiastic`. Nullable. |

> **Amendment — 15 August 2026, Lord Armand, from external architecture review.** Reaction is not intent. "He loved the idea" and "he approved the work" are different facts, and a schema that conflates them poisons Layer 5 distillation with false approvals. `reaction` is therefore recorded independently of `event_type`: a record like *reaction: strongly_enthusiastic, no acceptance event* is representable (`event_type` null) and queryable. A reaction is **never inferred from wording alone**, and **enthusiasm is never evidence of approval** — the same rule appears where distillation reads this table (`02-partner-systems.md` §2.1).

`reason_source` matters more than it looks. A reason Val inferred and a reason Lord Armand stated are different evidence, and Layer 5 distillation must be able to weight them differently. Collapsing them now makes every later lesson slightly untrustworthy.

**`deliberations`** — per `02-partner-systems.md` §4.7

| Column | Note |
|---|---|
| `id`, `created_at`, `project_id`, `conversation_id`, `message_id` | |
| `position` | Val's position, formed blind |
| `confidence` | `high` \| `medium` \| `low` |
| `reasoning` | Brief |
| `stripped_content` | What the strip step removed (§3.9) |
| `ordering` | `enforced` \| `contaminated` — whether the blind position was genuinely blind |
| `user_response` | |
| `outcome` | `updated` \| `held` \| `overridden` \| `agreed_from_start` |
| `what_changed_her_mind` | Nullable; required when `outcome = updated` |
| `both_positions`, `predictions` | Nullable. Populated on compromise. **This is the seed of the prediction ledger.** |
| `classification` | `consequential` \| `uncertain` |
| `classified_by` | `automatic` \| `user` \| `val` |
| `blind_position_id` | **19 August 2026.** The exact `blind_positions` evidence this record resolves. Nullable: a deliberation recorded manually, or one whose exchange carried no preference to strip, has no blind call behind it. |

**`blind_positions`** — amendment, 19 August 2026, Lord Armand

The blind position is the primary evidence that Val formed a genuinely independent judgment — Layer 5 exists partly to determine exactly that — and it exists *before* the exchange resolves, while the `deliberations` row cannot be written until the outcome is known. Keeping the most load-bearing record in the least durable place would be backwards, so the position is captured as an **append-only evidence row, persisted before step 3 of §4 begins**, on the same footing as every other Layer 0 capture table. It is not mutable interim state: no UPDATE, no hard delete, complete when written.

| Column | Note |
|---|---|
| `id`, `created_at` | |
| `project_id`, `conversation_id`, `message_id` | The exchange it belongs to. Project derived from the conversation's stored scope. |
| `model_call_id` | The blind call itself — the `model_calls` row that produced this position |
| `persona_id` | The persona revision assembled into the blind call (see WP-0.5's 19 August 2026 amendment) |
| `position`, `confidence`, `reasoning` | The position as formed, blind |
| `stripped_content` | What the strip step removed |
| `ordering` | `enforced` \| `contaminated` |
| `classification`, `classified_by` | The §4.8 provenance that triggered the capture |

The exact blind-call payload is additionally logged for WP-0.9's inspection criterion; the log is for inspection, this table is the evidence.

**`classifications`** — ruling, 3 September 2026, Lord Armand

The §4.8 classifier's verdict and declared reason were never persisted, and its `model_calls` rows carry no conversation or message id under the 18 August rule (provenance iff conversation), so a turn's classification could be tied to its call only by timestamp — and for an ordinary turn nothing durable said how it had been classified at all. The ruling adds a **separate, append-only, per-turn evidence record** and keeps `model_calls` exactly as it was: the turn linkage lives here. Written when classification concludes — established or not — and before any strip or response call, so it exists whatever the turn then does. It must answer, later, *how was this specific turn classified, and why?* for ordinary turns as well as consequential ones, from the classifier's own declared structured reason; nothing is inferred, and nothing historical is retrofitted from timestamps.

| Column | Meaning |
|---|---|
| `id`, `created_at` | As everywhere |
| `project_id` (nullable), `conversation_id`, `message_id` | The turn: project derived from the conversation, never supplied; `message_id` is the user message classified |
| `established` | Whether any permitted attempt stated a verdict; `false` ends the turn unanswered |
| `verdict` (nullable) | `consequential` \| `uncertain` \| `not_consequential` — the classifier's declared verdict, present iff `established` |
| `hard_exclusion` (nullable) | The exclusion the classifier named, if any — the other half of its declared reason |
| `attempts` | Calls permitted and made for this turn, at most two |
| `model_call_ids` | Every classification `model_calls` id for this turn, in order, including calls that failed after reaching a provider; may be empty when nothing was transmitted |
| `resolving_model_call_id` (nullable) | The call the verdict came from, or the final recorded attempt where none did |
| `resolution` (nullable) | Where unestablished: why, per attempt, in the orchestrator's own words |

Guarded by the standing functions: no UPDATE, no hard delete (migration `0013`). The conversation detail carries these rows; whether an ordinary turn's verdict is *displayed* is a WP-0.10 presentation decision not changed by this ruling.

**`classification_labels`** and **`classification_reviews`** — ruling, 7 September 2026, Lord Armand

WP-0.9's fifty-exchange criterion collects Lord Armand's own verdicts; nothing in the store could hold them. Two append-only tables (migration `0014`), with three amendments to the proposed shape ruled the same day:

| Table | Column | Meaning |
|---|---|---|
| `classification_labels` | `classification_id` | the classification labelled; **unique** — one original blind label, never overwritten |
| | `label` | `consequential` \| `uncertain` \| `not_consequential` — his verdict under §4.8, committed **before** the classifier's verdict is revealed to him |
| | `exclusion_determination` (nullable) | present iff `not_consequential`: one of the six hard exclusions, or the explicit `none_fails_inclusion_test`. An omitted determination is refused, never read as "none" |
| | `labelled_by` | `user` |
| `classification_reviews` | `classification_id`, `label_id` | the exchange and the label adjudicated |
| | `conclusion` | `label_upheld_classifier_wrong` \| `classifier_upheld_label_wrong` \| `ambiguous_needs_ruling` |
| | `reason` | stated, in words |
| | `tuning_state` (nullable) | present iff the classifier was wrong: `tuning_required` until a later row records `tuning_verified` |
| | `tuning_change`, `tuning_verification` (nullable) | present iff `tuning_verified`: the engineering change and how it was verified |

*Blind before reveal, structurally.* The review queue returns the exchange and never the verdict; the verdict and hard exclusion are returned only by the call that stores the label. *Scoring, derived on read and never stored:* agreement when label and verdict match; **zero tolerance fires only when the label named one of the six and the classifier said consequential**; every other mismatch, including `uncertain` against either verdict, is an inclusion-test disagreement. *Eligibility, applied by the service:* established classifications from live turns on or after the resume of 7 September 2026 (commit `b6d5c32`, 17:56 Central), oldest first; unestablished rows never enter the queue; classifications that accumulated before the interface existed remain reviewable in the same queue. No manufactured sample balance; the fifty are real exchanges of both kinds as use produces them. A disagreement is never resolved by being viewed.

### 2.3 Constraints

- Every `message` resolves to a `conversation`. Every `conversation` resolves to a `project` or explicitly to none.
- `execution_events` and `deliberations` cascade on nothing. They outlive the conversation that produced them. So do `ideas` and `idea_state_changes` (§2.4).
- No table permits hard delete at Layer 0. Corrections preserve lineage (`00-charter.md` invariant 14).

### 2.4 Ideas — amendment, 15 August 2026, Lord Armand

An idea's history cannot be reconstructed later, which is the same capture argument as §2.2. Layer 0 records it with **manual marking only** — no automatic idea detection, no classification calls. Machinery arrives with later layers.

**`ideas`** — `id`, `project_id` (nullable — same rule as everywhere), `title`, `lifecycle_state`, `created_at`, `updated_at`

**`idea_state_changes`** — `id`, `idea_id`, `from_state` (nullable — null marks creation), `to_state`, `changed_at`. Lineage is append-only: state history is preserved, never overwritten. `ideas.lifecycle_state` duplicates the latest `to_state` for query convenience; the application keeps them consistent, and the lineage rows are the record.

`lifecycle_state` values: `mentioned` | `discussed` | `researching` | `prototyped` | `approved` | `implemented` | `superseded` | `rejected` | `abandoned`.

Two rules, binding on every writer and on Layer 5 distillation:

- **`implemented` is never inferred from discussion of how something might be built.**
- **`approved` is never inferred from enthusiasm.**

### 2.5 Budget reservations — amendment, 17 August 2026, Lord Armand

Enforcing the ceiling against the call being proposed rather than against history (`01-architecture.md` §5.7 as amended) requires knowing what is already claimed but not yet settled. That is a fact about the house rather than about a process: `api` and `worker` share one gateway implementation and not one address space, and two counters would each observe the same headroom and together spend it. **PostgreSQL is the only place that can answer for both**, so this is a table.

**`budget_reservations`** — `id`, `created_at`, `updated_at`, `state`, `model_config_id`, `slug`, `provider`, `model_identifier`, `task_type`, `project_id` (nullable — same rule as everywhere), `max_cost`, `settled_cost` (nullable), `cost_certainty` (nullable), `model_call_id` (nullable), `resolution` (nullable)

`state` values: `reserved` | `settled` | `released` | `expired`.

| State | Means | Counts against the ceiling |
|---|---|---|
| `reserved` | Admitted; the provider is being contacted | Yes, at `max_cost` |
| `settled` | The attempt finished | Yes, at `settled_cost` |
| `released` | No provider request occurred | No |
| `expired` | The process died holding it | **Yes, at `max_cost`** |

Four rules, binding:

- **Admission is atomic.** The sum, the decision, and the insert happen in one transaction under a lock, so no second caller can slip between the sum and the insert. A check-then-act guard leaves exactly that window open, and two calls through it breach the ceiling by the size of one call.
- **`expired` still counts.** A reservation whose process vanished may or may not have reached the provider, and nothing on this machine can establish which. Freeing it would treat an unknown consequential outcome as a successful non-event, which `00-charter.md` §4 forbids in as many words. It stays committed, it is reported in words at startup, and it falls out of the sum when the month resets — so a crash costs at most the remainder of one month and never silently widens what may be spent.
- **An unknown cost settles at the full reserved maximum.** The provider was reached and would not say what it charged; releasing the difference would treat "we cannot tell" as "nothing was spent."
- **`settled_cost` may exceed `max_cost`, and if it does the record says so.** It should never happen — the reserved figure is an arithmetic upper bound, not an estimate — but the row is written truthfully rather than clamped. A tidy number concealing a breached ceiling is worse than the breach.

No hard delete, like every other table (§2.3). A spending record that can be deleted is a spending record that will be.

**Why this is a table when the Model Configuration Registry is not.** The registry is a versioned artifact whose history belongs in git — dated, diffable, attributable, and unable to drift once deployed. A reservation is the opposite: it is mutable state that two processes must agree on *right now*, and the whole mechanism is that agreement. Neither shape fits both.

---

## 3. Work packages

Each states what exists when it is done and how that is verified.

### WP-0.1 — Repository and toolchain

**Done when:** the repository structure of `01-architecture.md` §3 exists, every tool version is pinned to an explicit version resolved against current official documentation, and a clean checkout builds on a machine that has never seen the project.

**Verified by:**
- CI enforces dependency direction and fails on violation. Prove it: open a PR that references `desktop` from `policy`; CI must reject it.
  - *Amended from "imports" after PR #1: the rule governs dependency direction, not import syntax, and `policy` is Python while `desktop` is TypeScript and Rust, so no import statement in any language can express this edge.*
- No version placeholder remains anywhere. Grep for `TODO`, `TBD`, `latest`, and unpinned specifiers; result is empty.
- Clean-clone build succeeds from a documented command sequence with no undocumented step.

### WP-0.2 — Database and migrations

**Done when:** PostgreSQL with pgvector is running, §2's schema exists entirely through Alembic migrations, and the migration set applies cleanly from empty.

**Verified by:**
- `alembic upgrade head` against an empty database produces the full schema.
- `alembic downgrade base` then `upgrade head` succeeds — migrations are reversible.
- Schema matches §2 exactly. No table exists that §2 does not name.

> **Note on reversibility, 17 August 2026.** "Reversible" means the migration set has a defined and tested downgrade from an empty-database run, which CI exercises on every push. It has never meant that a downgrade may destroy capture records to succeed. Two migrations deliberately fail against real data — `0002` on reaction-only rows, `0003` on rows honestly recording an unknown cost — and that refusal is the correct behaviour, not a gap in reversibility. A rollback that erases what was captured is not a rollback.

### WP-0.3 — Backup and verified restore

**Done when:** automated encrypted off-machine backup runs daily, WAL archiving is enabled, and a restore has actually been performed and verified.

**Verified by:**
- A backup runs unattended on schedule with no human step. Confirmed by observing two consecutive days.
- Backups are encrypted, and the key is stored somewhere other than the backup location. Demonstrate by restoring on a machine holding only the backup — it must fail without the separately-held key.
- **A full restore to a scratch instance is performed and checked:** row counts match source per table, referential integrity holds, and conversation, execution history, and deliberation records are continuous with no gaps.
- Point-in-time recovery to an arbitrary timestamp within retention succeeds.

**This package does not pass on a backup that has been configured but never restored** (`00-charter.md` invariant 35).

> **Amendment — 7 September 2026, Lord Armand. The backup window moves from 03:00 to 20:00 local.** Nightly runs grew from about forty seconds to sixteen minutes, six hours (the Sunday full of 6 September), and forty-eight minutes, with one run aborted on the write-ahead-log archive timeout. Established cause: the laptop was asleep on battery, and macOS advances a scheduled job only inside dark-wake maintenance windows of about three minutes every sixteen; every burst of file copies in the pgBackRest log aligned with a dark wake, and most of the 6 September copies completed in the three minutes after the machine fully woke. Nothing about pgBackRest, Backblaze, PostgreSQL byte placement, or backup architecture is reopened. PostgreSQL stays online and in normal use during the run. The next several unattended runs are observed; abnormal duration or reliability while the machine is awake is reported.

### WP-0.4 — Model Gateway

**Done when:** all inference routes through one internal gateway, at least two providers are wired, every call is costed and attributed, and the hard stop works.

**Verified by:**
- No provider SDK is imported anywhere outside `packages/providers`. Enforced by CI.
- Two providers answer the same request through one normalized contract. Swapping the configured provider requires no change outside configuration.
- Every call writes a `model_calls` row with cost, project, and task type populated. Zero calls without a row — verified by comparing provider dashboards against the table for a day of real use.
- Provider errors, timeouts, and refusals normalize to one error contract. Test by pointing a configuration at an invalid endpoint and at a request that will be refused.
- **Hard stop:** with month-to-date cloud spend seeded above the ceiling, cloud routing stops and Val says plainly that it has. Test with a seeded value; do not wait for it to occur naturally.
- **The ceiling is enforced against the proposed call — amendment, 17 August 2026.** With spend seeded at $199.99 and a call authorised to consume more than the remaining $0.01, the provider is **not contacted**. Two simultaneous calls competing for insufficient headroom admit at most the authorised amount between them, proved against a real PostgreSQL rather than a fake. A reservation released, expired, or settled below its maximum returns exactly what it should, and an expired one returns nothing. Mechanism: `01-architecture.md` §5.7 and §2.5 above.
- **Cost accounting is truthful — amendment, 17 August 2026.** A rejection before the provider is contacted writes **no** `model_calls` row. A provider failure carrying no usage is recorded as `cost_certainty = 'unknown'` with NULL figures, never as a zero, and its reservation stays charged. §2.2 above.
- **The gateway routes — amendment, 17 August 2026.** A caller states what the content is and what the work is; the gateway selects the configuration. It selects only among configurations admitted for Layer 0 and eligible for the classification; a cheaper ineligible configuration is never selected; a fallback is used only if it independently satisfies every rule; and where no route qualifies the result is a normalized, truthful unavailability, never a downgrade. Passing an arbitrary provider and model identifier through an application path does not create a route.
- **Eligibility is enforced at startup, not at call time.** Configuring a provider not declared eligible for Protected content causes startup to fail with a clear error. Test by adding an ineligible configuration; the service must refuse to start. A check that only fires when the call is made is not the guarantee §1.1 claims.
- Restricted content is refused rather than routed. Test with content classified Restricted; Val declines and explains, and no `model_calls` row is written.
- **Restricted preflight — amendment, 15 August 2026, Lord Armand.** A deterministic local check reads the *content* before any cloud transmission, because the stated classification is only as good as the caller's knowledge. It runs before the provider is contacted, never uses the receiving model to classify, **blocks rather than downgrades**, fails closed if the check itself errors, writes no `model_calls` row (no call occurred), records the block, and explains plainly. Deliberately small: obvious credentials, keys, connection strings, government identification, and Luhn-valid payment cards. It is **not** the Layer 2 per-content classification system arriving early.

> **Amendment — 7 September 2026, Lord Armand. The quality floor is enforced in routing: capability profiles.** The cost and latency report of this date found that "the gateway routes" above was implemented as eligibility → cheapest, so every task including Val's own voice ran on the cheapest eligible route — the classifier's route. That is the silent step-down the standing rule of `01-architecture.md` §5.5 (2 September) forbids, reached through ordering rather than through the ceiling. Ruled **fix now**, Lord Armand's substantive work paused until deployed and demonstrated: each configuration declares the capability profiles it satisfies (`partner`, `structured`), each task type requires one (conversation and blind position → `partner`; classification, strip, title → `structured`), and the router applies **eligibility → required profile → readiness, policy and budget → cost among routes satisfying the floor**. The exact same-configuration pin between blind position and response is preserved and now sits inside the floor; a pinned configuration below the floor is refused. **If no partner-qualified route is available the call fails honestly, naming the floor — never falling back to a structured route.** Provisional partner qualification is a ruling: as of this date `opus-5` alone declares `partner`, as an implementation bridge, not a claim that it is uniquely or permanently correct; qualifying any other route is a separate ruling, and no model name appears in routing logic. Verified by: conversation and blind position route to a partner-qualified route, never to the cheapest overall; classification and strip route to the cheapest structured route; a cheaper structured-only route never wins a partner task; a partner route's structured fallback never serves a partner task; with no partner route ready, the failure names the floor; startup warns when nothing admitted declares `partner`. Demonstrated against the real provider on deployment, with measured cost, tokens and latency for a fresh ordinary turn and a consequential one (`docs/reviews/VAL_Test_and_Evidence_Index.md` §12).

> **Amendment — 8 September 2026, Lord Armand. Prompt caching on the partner route — the economic lever that keeps the partner floor usable.** The measured ordinary turn after the quality floor cost about $0.04, of which the persona — some 5,700 tokens re-sent whole on every partner call — was most of the input. Ruled: caching on the stable prefix, without trimming, summarising, or weakening the persona; qualification of further partner routes proceeds in parallel, because caching alone cannot reach the long-term conversational target. Built: **(1)** the registry declares, per configuration, verified cache rates read from the provider's pricing page and the model's minimum cacheable prefix (`caching = AVAILABLE` iff the rates are present); **(2)** the Anthropic adapter places one cache breakpoint on the `system` text — the persona, whole, the only byte-identical prefix every partner call shares; nothing in `messages` is marked, because the memory envelope varies per turn and precedes the history; **(3)** the lifetime is configuration, `VAL_CACHE_TTL` (`5m` | `1h`; unset requests nothing and startup warns; an undocumented value stops startup); the gateway requests it only when the route's caching is verified and the prefix meets the model's minimum; **(4)** **the reservation never assumes a hit** — the bound prices every input token at the greater of the base rate and the requested lifetime's write rate; **(5)** settlement prices the provider's four usage figures (uncached input, cache writes by lifetime, cache reads, output) at the verified rates, stores the total at call time, and persists the split (§2.2, `model_call_cache_usage`). Retention: Anthropic's retention page marks prompt caching ZDR-eligible — "your prompts and Claude's outputs are not stored; KV cache representations and cryptographic hashes are held in memory for the cache TTL and promptly deleted after expiry" — and caches are isolated per organization and workspace; read as within the existing Protected-eligibility premise (content not retained at rest, never trained on), and recorded here so the reading can be overruled. The 17 August 2026 warning in `maximum_cost` — "whoever enables caching must widen this formula in the same change" — is discharged by (4). Measured cold and warm figures: `docs/reviews/VAL_Test_and_Evidence_Index.md` §13 and `docs/reviews/VAL_Ordinary_Conversation_Economics_Report.md`.
>
> **Amendment — 13 September 2026, Lord Armand. Blind-position calls request no prompt cache.** Every blind-position call on record created a fresh cache entry and none ever read one. The adapter's breakpoint already sat at the end of the persona text; the blind call's cached prefix was nonetheless always 265 tokens longer than the persona prefix the response route caches (5,746 → 6,011 under persona v1.4; 6,396 → 6,661 under v1.7), a constant consistent with the structured-output format's own system text, which the provider renders inside that prefix and which the application cannot move behind a breakpoint without removing the output contract. Reuse of the response route's persona cache is therefore impossible without changing the blind request, and the fallback ruling applies: **the gateway requests no cache lifetime for `blind_position` calls** (`Gateway._cache_ttl_for`). The blind request is otherwise byte-equivalent — only cache-control metadata is not sent — and the persona, attribution, route (`opus-5-medium`), effort, schema and evidence behaviour are unchanged; the reservation is the base-rate bound. Response, strip and every other task type decide caching exactly as before. On the 13 September blind call this is $0.070880 instead of $0.104185.
>
> **Amendment — 13 September 2026, Lord Armand. Exchange identity, per-call measurement, a disabled exchange envelope, and a bounded blind instruction.** On the accepted cognition-cost diagnostic (`docs/reviews/diagnostics/2026-09-13-cognition-cost-and-latency.md`). *Exchange identity:* every `GatewayRequest` an exchange makes — classification, strip, blind position, the response, and their retries — carries the exchange (`GatewayRequest.exchange`, the existing `TurnReference`; a conversation call's exchange is its provenance), and the ledger records it on `budget_reservations` (`exchange_conversation_id`, `exchange_message_id`, nullable, both or neither, pinned by the reservation identity guard; migration `0020`). Machinery rows on `model_calls` keep their no-provenance record; `model_calls` is unchanged. Historical rows are not backfilled. *Measurement:* an append-only sidecar `model_call_measurements`, one row per call written in the call's transaction — exchange, whether streamed, time to the first generated-text delta at the Val Core boundary, generated-text characters, and only where the provider exposes them reasoning presence, reasoning tokens, and prompt-cache reads and writes as reported; never inferred, never zero for unknown. *Exchange envelope:* `DatabaseLedger` sums an exchange's settled and outstanding reservations (settled at settled cost, outstanding and expired at maximum, released at nothing) under the existing advisory lock and refuses a reservation that would exceed an optional envelope with `EXCHANGE_ENVELOPE_EXCEEDED` — not retryable, never a cheaper route, proceeding needs owner authorisation. Configured by `VAL_EXCHANGE_ENVELOPE_USD`; **unset, the default, is disabled**; no threshold is chosen. *Blind instruction:* "Commit: name the option you would choose and why, briefly." is replaced by "Commit: name the option you would choose, in at most two sentences, and give only the material reasons for it — at most about 120 words of reasoning. Do not restate the question, draft the work itself, or add caveats that would not change the position." Schema, parser (no length rejection), the 4,096-token ceiling, attempts, persistence, reconciliation envelope, payload logging and the strip rules are unchanged. Evidence index §58.
>
> **Amendment — 14 September 2026, Lord Armand. Automatic provider caching priced; the candidate lane.** *Caching:* a provider that caches automatically — GPT-5.6, writes at 1.25× the input rate on a 30-minute minimum lifetime, reads at 0.1×, 1,024-token minimum, reported as `cached_tokens` and `cache_write_tokens` — is represented by one verified automatic write rate on the registry entry (`cache_write_auto_per_mtok_in_usd`, exclusive of the requested-lifetime pair; Anthropic's semantics unchanged). The adapter reports uncached, read and written input disjointly; settlement prices each at its verified rate — `uncached × base + written × write + read × read + output × out`, the long-context multiplier stacking on all three — never a token twice, missing usage still unknown; on an unverified route every reported figure is priced at base. The cold reservation bound prices **every** input token at the write rate, the legitimate expensive state, assuming no hit. No lifetime is requested from such a provider and no `model_call_cache_usage` row is written; the reported reads and writes are on the measurement row. `gpt-5-6-sol-medium` is verified at $4 / $0.40 / $5 / $20. *The candidate lane* (`01-architecture.md` §5.2 ruling of this date): `QualificationTarget` is a distinct type, never a capability profile; only a `NOT_ADMITTED`, profile-less entry may carry one, and it satisfies no required profile, makes nothing active, routable or a fallback. A `CandidateGateway` — a separate type built only by `candidate_gateway_for_scratch_store`, which refuses any database not named `*_test` — exercises such an entry on the two tasks in which Val speaks with every ordinary check in force (persona whole and attributed, Restricted preflight, provenance, eligibility, reservation with exchange identity, settlement, measurement, no fallback), recorded under the candidate's own identity; `deliberate.send(candidate=...)` pins only the blind position and the response to it, classification and strip routing as always, and a plain gateway refuses the argument. Production startup builds a plain `Gateway`; no HTTP contract names a configuration; the structured evaluation door still refuses conversation and blind position. Passing confers nothing; admission remains a separate ruling. **The first A2 call awaits authorisation.** Evidence index §59.

> **Amendment — 8 September 2026, Lord Armand. Cost among adequate routes is the total cost bound of the call.** The multi-provider report found the router ordering adequate routes by input rate alone, with an alphabetical slug tie-break — so two routes at the same input rate would have been decided by their names. Ruled: once eligibility, the capability floor, readiness and the other governing constraints have produced adequate candidates, they are ordered by the **candidate-specific total cost bound for the actual call** — input bound plus output bound at that candidate's rates, using the same input accounting and task-specific maximum-output allowance the reservation and admission already use (`maximum_cost`). No expected output length is invented. An exact tie falls to a stable deterministic last-resort tie-break (slug), and **the gateway logs that a true tie occurred**; the choice is never described as a cost decision. No live effect while one partner route exists; fixed now so that qualifying a second route cannot activate arbitrary provider selection.

### WP-0.5 — Persona loading

**Done when:** `03-persona.md` loads whole into every context from the active `personas` row.

**Verified by two separate checks.** The persona loads from the active `personas` row, so comparing the assembled context against the source document would let a divergence between file and row read as a pass.

- **Check one:** the assembled context byte-matches the **active `personas` row**. No truncation, no summarisation.
- **Check two:** the active row byte-matches `03-persona.md` **at seed time**, verified when the row is seeded and whenever the document changes.
- Editing the persona creates a new version row and leaves the prior row intact.
- Val's register in a real exchange is recognisably that of `03-persona.md` §9. Assessed by reading, not asserted.

> **Amendment — 19 August 2026, Lord Armand. "Every context" is deliberately narrowed to every context in which Val speaks.** WP-0.9 is the first place non-conversation model calls exist, and the rule is resolved rather than left to be read two ways: the **blind-position call carries the active persona whole** — the position must be *Val's* position, and a persona-less blind position compared against her persona-bearing response is two different speakers, which she would update away from nearly every time. That is the same failure `02-partner-systems.md` §4.1 names for a weaker model forming the blind position — theatre generating a paper trail of false independence — arriving through a different door. **Classification and strip calls carry no persona**: they are the house reading content, not Val speaking. This is a narrowing recorded as a decision, not an omission. Every call that carries the persona attributes it: `model_calls.persona_id` names the revision, verified against the active row before transmission.

> **Amendment — 9 September 2026, Lord Armand. Loaded-persona hygiene.** Persona v1.3's in-document change log carried a qualification test prompt, earlier model answers and the scoring criterion into every partner call, which confounded the evidence taken under it. Ruled, durable: **historical rulings, prior model answers, test prompts, qualification results, and audit or change-control records must not be injected into the loaded persona.** Those live in governing or evidence documents outside ordinary partner context; the persona's change history lives in `03-persona-changelog.md`, which is never loaded. The persona defines how Val speaks and behaves, and nothing else rides in it.

### WP-0.6 — Project resolution and attribution

**Done when:** every exchange is attributable to a project or explicitly to none, and resolution is deterministic.

**Verified by:**
- Resolution uses explicit names, IDs, and session state. Application code sets final scope; no model output determines it.
- Ambiguous project reference produces a question, never a guess. Test with a name matching two projects.
- No message exists with an unresolved project state — nullable means "explicitly none," and the distinction is queryable.

### WP-0.7 — Conversation loop and memory

**Done when:** a real conversation persists across a full application restart and Val recalls prior context within a project.

**Verified by:**
- Full restart of application and database mid-conversation; conversation resumes with history intact.
- **Automatic** retrieval is project-scoped. A query in project A returns nothing from project B through automatic recall. Test with deliberately similar content in both. *(Amended 12 September 2026: House Recall, the explicitly triggered cross-conversation path, is the only authorised exception — see the amendment of that date below.)*
- Message ordering is stable and gapless under concurrent writes.
- **Trap questions — amendment, 15 August 2026, Lord Armand.** With the database seeded with discussion and enthusiasm around a fictional decision that was never approved, "when did I approve X?" is answered with a correct negative — *"I find discussion and enthusiasm, but no approval record"* — never a confabulated date. At least three cases, each run against the real retrieval path and never against mocks: **never-approved**, **approved-then-superseded**, and **mentioned-once-then-abandoned**.

> **Amendment — 7 September 2026, Lord Armand. Recall is bounded by count and by a soft token budget.** A live turn's response input reached 34,365 tokens, of which about 28,000 were six recalled messages quoted whole — recall was bounded by count alone. Ruled: recall stays verbatim and unsummarised; the maximum of six messages stays; a configurable **16,000-token soft budget** (`VAL_RECALL_TOKEN_BUDGET`, a recall-context target, never a capability or spending limit) is added with these selection rules — rank for relevance exactly as before, size never affecting rank; always admit the highest-ranked message whole, even when it alone exceeds the budget, subject only to the provider context preflight; consider the rest strictly in rank order, admitting a whole message only if it fits the remaining budget; **stop at the first that does not fit, never skipping it to admit shorter lower-ranked messages**; never summarise, paraphrase, or truncate. Tokens are estimated locally from characters at a ratio calibrated on the measured turn; no provider is asked to count. Every selection is logged with each candidate's rank, size, admission, and reason, and the resulting recall size. *Unit, confirmed 8 September 2026:* the estimate is in provider-context-token scale (`val_policy.tokens`), so 16,000 admits approximately 16,000 provider-counted tokens — the magnitude ruled. *The unit is superseded 13 September 2026 (the amendment of that date below): recall admission is now a 16,000-byte limit over the exact serialized recall envelope; this paragraph is kept as the history of what was ruled.*

> **Amendment — 8 September 2026, Lord Armand. A result with no valid user-visible text never becomes a Val message.** The prompt-cache demonstration of this date found two provider refusals with zero output tokens persisted as empty `val` messages: the 18 August closure-pass doctrine ("a deliberate refusal is Val's answer") assumed a refusal carries words. Ruled, the same class as the classifier and the blind position failing silently to ordinary: when the text to be spoken is empty, the turn **ends unanswered**; the user message and the `model_calls` row are preserved; the adapters capture the provider's own terminal fields (`stop_reason`, `stop_details` — a refusal's category and explanation, an incomplete reason) and the observed cause is surfaced **outside Val's voice**; where the provider gave a recognised reason it is named, where it gave none the cause states only that no valid assistant content was returned, and no explanation is invented; **no assistant message is persisted in either case**. A consequential turn keeps its blind row and writes no deliberation. Regression: textual success, zero-output refusal, zero output with a recognised terminal reason, zero output with no recognised reason, and the consequential case (`packages/gateway/tests/test_empty_response.py`).

> **Amendment — 9 September 2026, Lord Armand. The prior-record state is a mandatory, typed part of every partner response call.** The v1.5 qualification reveal found the residual unsupported-continuity class no longer a persona problem: an empty record reached the response model as silence, and silence does not distinguish "nothing available" from "not supplied". Ruled and built: the memory envelope (`VAL-MEMORY-V1`) is **always present** on every partner response call, carrying a gateway-produced `prior_record_state` in compact typed form — same-conversation history (`available` | `zero`, prior and retained counts), retrieved excerpts (`returned` | `zero` | `not_run` | `unavailable`, with count and failure class), and project volumes (`not_applicable`, 0, until a volume mechanism exists) — followed by the excerpts, empty when none. **Absence and uncertainty are never collapsed:** a retrieval that ran and returned nothing says `zero`; one that was not run, failed, or does not apply carries its own state. The state describes what is available to *this call*, not what exists elsewhere in the house, and the note says so. Produced from the store by `val_gateway.loop.assemble_turn` and `val_gateway.context.PriorRecordState`, never by a model; logged per call (`prior record state`). One behavioural consequence, consistent with invariant 25: a retrieval failure now degrades the call to `unavailable` rather than ending the turn, and the envelope says so; a cross-project leak still raises. Demonstrated deterministically for the empty, populated, zero-result, unavailable and not-run states (`test_conversation_memory.py`, "the prior-record state"). The blind-position call is unchanged: it carries the stripped question only, by the 7 September ruling.

> **Amendment — 12 September 2026, Lord Armand. House Recall: the explicitly triggered cross-conversation retrieval path.** Conversation and project attribution answer *where a conversation belongs*; recall authority answers *what prior House records Val may deliberately consult when the present request calls for them*. These are separate concepts. Three retrieval paths, kept distinct: the current conversation's ordered history (unchanged); **automatic recall**, which remains project-scoped, with an explicitly no-project conversation a clean room for it (the two governing statements above are amended to say *automatic*; the isolation tests proving that Project A automatic recall cannot receive Project B material stand unchanged); and **House Recall** — a separate path that searches every stored conversation except the current one, across projects and the unassigned pool, and is the only authorised exception created by this ruling. House Recall is access to relevant prior conversation, never universal injection of House history. **Applicability is deterministic** (`val_policy.recall_gate.gate_house_recall`): no provider call; a closed, version-controlled inventory of explicit cross-conversation references ("we discussed", "what did we decide", "you said earlier", "remember when", "what do you remember", "earlier/previous/prior conversations", "from another project", "search … conversations" and their listed variants); an unmatched message is *not run*; a Tier One form never runs it; a quoted reference every span of which is found verbatim in the retained thread is thread-grounded and does not run it; `gate_recall` is never altered; the decision and its reason are recorded. Not a semantic classifier and never model-mediated. **Retrieval** (`val_gateway.memory.house_recall_with_state`): the same full-text ranking, limit and aggregate budget as automatic recall; results deduplicated against automatic results by message identity; every excerpt carries `retrieval_path`, `source_scope` (a named project or `unassigned`), `created_at`, and the existing message, conversation, project, sequence and role provenance; conflicting statements from different conversations remain separate excerpts with their chronology, never collapsed. It never changes the current conversation's `project_id` and never reattributes history. Recalled content remains historical data under the `VAL-MEMORY-V1` framing; Restricted preflight, eligibility, the budget ceiling, provenance and the instruction/data boundary apply to the assembled payload exactly as before. **The record-state contract is extended additively**: a sibling field `house_recall: {state, count, detail}` in the existing vocabulary (`returned` | `zero` | `not_run` with the gate's reason | `unavailable`); `retrieved_excerpts` keeps its meaning for automatic recall; the memory envelope's note states that excerpts from different conversations can conflict and are read by provenance and chronology. No schema migration. Deterministic evidence: `test_house_recall_gate.py`, `test_house_recall.py`; the bounded live verification is recorded in `docs/reviews/VAL_House_Recall_Verification.md`.
>
> **Amendment — 13 September 2026, Lord Armand. House Recall grounding continuity, carried as provenance.** On the genuine House Armand conversation of that date, House Recall supplied three excerpts and Val made factual statements from them; on the next turn she received her own answer and nothing saying it had been grounded, and correctly called her statements unverified. The recall envelope is per call and was never persisted, and the selection was only logged, so no durable record could establish the mapping. Added (migration `0019`): **`answer_recall_sources`**, one append-only row per House Recall source admitted to the response call that produced a persisted Val answer — the answer, the call, the retrieval path, the rank, the source message, conversation and sequence, **the exact wording the response saw** (`source_revision_number`, NULL for the original, so a later correction cannot re-point it), the scope the source was written in, when it was said, and a title snapshot for presentation (identity is by id). A coherence trigger refuses an answer that is not Val's, a source in the answer's own conversation, a source that disagrees with its message, a revision number that is not a revision of the source, and a call from another conversation. On a later turn whose retained history contains such an answer, the record-state envelope carries `grounded_answers` — the answer's position, `house_recall` support, and each source's provenance with `status_now` (unchanged, corrected since, withdrawn since, conversation removed since) — **and never the sources' content**; a withdrawn or removed source keeps its historical support role without becoming live intent; House Recall does not run because the fact exists; no provider call is added; the key is absent when there is no such answer. Answers from before this ruling carry no grounding fact and are not backfilled.
>
> **Amendment — 13 September 2026, Lord Armand. Recall admission is bounded in bytes over the exact serialized envelope.** *Governing finding:* the budget of 7 September was a soft recall-context target of about 16,000 provider tokens under the local estimator, whose purpose was to prevent crowding and single-source dominance — never a capability or spending limit, and never a context-window guard; no provider was to be asked to count. On the House Recall opening turn of 13 September the estimator broke its own premise of approximately 16,000 and erring toward less: three excerpts estimated at 12,204 tokens were about 20,500 provider tokens by inference from the call record (43,939 envelope bytes), and the damage was crowding — the recall envelope was about three quarters of the response input, and one screenplay-formatted draft about 97% of the recall. *Superseded:* **recall admission is now governed by a conservative 16,000-byte limit over the exact serialized recall envelope.** This is an intentional reduction in recall capacity, not a 16,000-token budget. *Rule* (`val_policy.recall.select_within_envelope`, one policy for automatic recall and House Recall): in relevance order, serialize the envelope that would be transmitted — everything already admitted plus the next whole candidate — with the transmitting serializer (`val_gateway.context.recall_block`), measure its UTF-8 bytes, admit the candidate if the result is at most the limit, and otherwise stop under the existing semantics (a top candidate that does not fit is `top_candidate_exceeds_budget`; a later one stops selection; nothing is skipped, substituted or truncated), all before transmission. Ranking, chronology, whole excerpts, provenance, project isolation, Restricted handling, the six-message limit per path and the record-state vocabulary are unchanged. Because both paths' excerpts travel in one envelope, House Recall selection begins from the excerpts automatic recall already admitted, and the transmitted envelope as a whole stays within the limit. *No provider counting:* no `count_tokens`, no counting task type, sidecar, eligibility, egress, fallback, speculation or network dependency; no multiplier, screenplay exception, larger byte limit, tokenizer approximation or fallback count compensates for the reduced capacity. *Configuration:* `VAL_RECALL_ENVELOPE_BYTES` (default 16000); the estimated-token `VAL_RECALL_TOKEN_BUDGET` is retired and refused at startup if set, so a value chosen in tokens can never silently govern a limit in bytes. The history budget (≈64,000 estimated tokens), the context-window preflight and the reservation are unchanged. Implementation record: `docs/reviews/VAL_Test_and_Evidence_Index.md` §55.
>
> **Amendment — 13 September 2026, Lord Armand. The record-state envelope states operational capability; recall priority recorded, not changed.** On the genuine House Armand conversation of that date Val promised twice to start a book although no book mechanism exists (Books are Layer 5). *Governing finding:* nothing in her context told her which operations were actually available on the call; the persona describes her identity as keeper of the house's books, and identity is not an operational capability declaration. Added, additively: **`capability_state`** in the `VAL-STATE-V1` record-state document, `{"books": "unavailable"}`, with its meaning stated in the envelope's note (`unavailable` means the operation cannot be performed, begun or promised on the call, and says nothing about whether it will ever exist). It is a constant of the build (`val_gateway.context.CAPABILITY_STATE`), present on every partner response call before generation, never provider-generated or inferred, costing no call; it names only user-facing operations whose availability bears on her conduct — today books alone — and is not a tool planner, agent registry, capability-discovery service or roadmap. Every other record-state key is unchanged. The conduct half is persona v1.8 (`03-persona-changelog.md` §18). *Recall priority, observation only:* automatic recall occupies the shared recall envelope before explicit House Recall; if genuine use shows an explicit cross-conversation request crowded out by automatic recall, that occurrence is sufficient to reopen priority ordering. No change now.

> **Amendment — 10 September 2026, Lord Armand. Cross-conversation recall is conditional; the aggregate recall budget is a ceiling; the outbound order caches the history prefix; history eviction has hysteresis.** Ruled on the turn-path inventory (`docs/reviews/VAL_Turn_Path_Inventory.md`) under the per-turn necessity rule (`01-architecture.md` §5.5). Same-conversation history is never gated, and "no recall needed" never means "no context needed".
>
> *The recall gate* (`val_policy.recall_gate`): deterministic, no model, no provider call; it gates only whether cross-conversation retrieval runs and fails toward recall. Exactly one positive reason, in precedence: **`no_project_scope`** — an explicitly no-project conversation is a clean room **for automatic recall**: the unassigned pool is not searched automatically at all (existing unassigned records untouched; amended 12 September 2026 — House Recall, explicitly triggered, is the one authorised exception); **`tier_one`** — the *complete* normalised message is one of a finite whitelist of closed forms (`hello`, `hi`, `thanks`, `thank you`, `yes`, `no`, `okay`, `ok`, with the addresses `Val` / `my lord`; at most 15 ruled lexical words, no digits, quotation marks, question mark, capitalised token outside the vocabulary — sentence-initial position exempts nothing — or closed-inventory back-reference; the negative checks are necessary, never sufficient; no synonyms are inferred); **`tier_two`** — a thread-local message whose every referent is mechanically grounded in the retained history Val will actually receive on this call or in an exact envelope fact, with at least one positive anchor (a quoted string occurring verbatim in retained history with terminal punctuation inside the quote treated as orthography; a non-initial capitalised token grounded by exact token identity in retained history or the envelope; an immediate-thread back-reference — `again`, `the other`, `that one` — with a prior Val message to refer to; an envelope fact such as the time); length and digits relaxed; a question mark still recalls; every other inventory back-reference is a cross-thread signal and recalls. *Reading corrected the same day (ruling on the first implementation):* Tier Two has **no general sentence-initial exemption** — a sentence-initial proper noun can be a new external referent, and once another anchor exists a broad exemption would let recall be skipped with an unresolved referent. Every capitalised token outside the safe vocabulary must be grounded by the exact mechanical rule, or Tier Two fails toward recall; the closed orthographic exemption is `You` and `Please`, and sentence-initial `It` is nonreferential only at the head of the mechanically recognisable local-time assertion `It is <HH:MM>` of the captured regression — syntactic, never broadened to other sentences beginning with "It". Nothing infers whether an unknown capitalised word is a proper noun. Tier One keeps its strict rule. A gated retrieval is `retrieval_state = not_run` with its reason in the envelope; **`under_specified_query`** is reserved for a query from which no usable retrieval term could be formed (today: an empty query; the term-quality stage is not deployed, see below).
>
> *The aggregate budget:* the 7 September rule that the top-ranked candidate is admitted whole even when it alone exceeds the soft budget is **superseded**. No candidate may exceed the aggregate budget; a top candidate that does admits nothing from that ranking, is not truncated, has no shorter candidate substituted, and is recorded as `top_candidate_exceeds_budget` (a `zero` with that detail, never an unexplained zero). Whole-message integrity and the stop-at-first-misfit rule stand.
>
> *Term quality — ruled 10 September 2026: deploy none.* Assessed on the store, read-only (`VAL_Turn_Path_Inventory.md` §6–§7): neither captured false-positive recall is removed by any fraction floor tested (10%, 25%, 33%); both turns are already skipped by the gate and would be no-project clean-room skips regardless; in a focused real project a fraction floor drops topic terms (`lens`, 39% of Lighthouse Restoration's messages) and turns an explicit cross-thread request into `under_specified_query` — the ordinary case in a focused project, not the edge; and the screenplay false positive survives every floor through a shared selective word — only the gate stops it. **Recorded so the bypass removal is not later credited with it:** the superseded top-candidate rule would not have excluded the screenplay either, since at 10,830 estimated tokens it fits the 16,000 aggregate budget; on that turn only the gate prevents it. The 10/25/33% analysis and the suggested large-scope shape (apply only at fifty or more messages; drop a term only above half) are design evidence, not authorised. Term quality is revisited only if real project use produces a concrete retrieval-precision failure that survives the gate and the aggregate budget. The AND-to-OR rewrite is not reopened.
>
> *Outbound order and the cache breakpoint:* persona (`system`, breakpoint) → retained same-conversation history, its last message carrying a second breakpoint (`Message.cache_breakpoint`; the Anthropic adapter sends it as a text block with `cache_control`) → recalled excerpts, if any, framed as historical source with provenance and speaker → the record-state envelope, now its own block (`VAL-STATE-V1`) carrying the prior-record state and the clock → the current user turn. Recalled material is never promoted to system authority or live instruction. The reservation already bounds every input token at the write rate, so the second breakpoint changes no ceiling arithmetic.
>
> *History hysteresis:* both ceilings stay (≈64,000 estimated tokens; 40 messages under the selector's existing convention, which counts the current turn's message). When appending would require rolling eviction at a binding ceiling, the window is rebased once to a contiguous newest tail at ≈75% of that ceiling (≈48,000 tokens; ≈30 messages; the tighter when both bind), whole exchanges preserved, rule 6 intact, then appended to until a ceiling is approached again — never one oldest exchange per turn. Persistence of "rebase once" is achieved **without new state**: `val_policy.history.select_history_tail` replays the conversation's stored sequence from its start applying the same rule at each turn, so the boundary is re-derived identically on later turns and across application and database restarts; older pre-rebase exchanges are never refilled. Every rebase is logged with the selection. The deterministic classifier pre-filter and any partner-call parallelism are **not** built (deferred by the same ruling; `01-architecture.md` §5.5).

> **Amendment — 7 September 2026, Lord Armand. The history window is a contiguous tail under a soft budget.** The same report found the history window bounded by count alone (forty messages), so sustained long messages could grow it past a provider's context and hard-fail the turn. Ruled: the forty-message maximum stays, and a configurable **64,000-token soft budget** (`VAL_HISTORY_TOKEN_BUDGET`) is added. **Unit, corrected 8 September 2026, Lord Armand:** the budget is in **provider-context-token scale** — the approximate token magnitude represented to the provider — estimated locally by the one documented deterministic estimator (`val_policy.tokens`: characters ÷ 3.6, calibrated on a measured live call, erring toward admitting less), the same estimator recall uses. The first implementation measured it in the byte upper bound the preflight uses, which is about four times the provider's count and so admitted roughly a quarter of the ruled magnitude; that was not the magnitude ruled and was corrected the same day. The byte bound stays where it belongs, in the context-window preflight and the reservation. The figure is configuration, re-ruled without code. The rules: start with the newest historical exchange and work backward; preserve whole messages and complete user/Val exchange boundaries wherever the stored sequence permits, so the retained tail never begins with a Val answer whose triggering message was dropped; add older complete exchanges while the result stays within the budget and the forty-message maximum; **when the next older exchange would exceed the budget, stop — never skip it to include still-older shorter material**; never relevance-rank history, create holes in the retained tail, summarise, paraphrase, or truncate; if the newest complete exchange alone exceeds the budget, preserve it whole and include no older history, subject to the existing provider-context preflight. The current turn's own user message is always retained. Every selection is logged with each exchange's size and fate.

### WP-0.8 — Execution history capture

**Done when:** every acceptance, rejection, revision, and correction writes an `execution_events` row with its reason.

**Verified by:**
- Accept, reject, request revision, and correct — one of each in real use — and confirm four rows with correct types.
- A rejection without a stated reason prompts for one. Declining to give a reason records `reason_source = absent` rather than fabricating one.
- `reason_source` correctly distinguishes stated from inferred across a sample of twenty real events, checked by hand.

> **Amendment — 14 September 2026, Lord Armand. The twenty-event hand-check is complete.** The live store held 21 genuine real-use `execution_events` rows (31 August – 11 September 2026; no engineering, scripted, qualification, harness or scratch-store traffic among them). The owner sample is rows 1, 2 and 4–21 in `created_at` order; row 3 (31 August 22:57:10, `rejected`) is excluded from the sample because it duplicates row 4's reason on the same reply while carrying the interface's default `rejected` type — the row itself is unchanged, as every row is. Every sampled row is `reason_source = stated`, and the owner confirmed that reading: each stored reason is text he supplied; a reason that contains instructions or evaluative language is still his stated words, not an inference. All four event types are present in genuine use (`rejected` 31 August; `accepted` 31 August – 11 September; `revision_requested` 3 September; `corrected` 10 September, twice). **Structural finding, recorded and ruled:** the desktop control sends the typed reason or an explicit declination and never `reason_inferred`, and Val has no writer of her own at Layer 0, so real use produces only `stated` or `absent`; no `inferred` or `absent` row exists. The criterion is a hand-check of `reason_source` across twenty genuine events, not one example of every value — **no event is manufactured to obtain an enum value.** Evidence: `docs/reviews/VAL_Test_and_Evidence_Index.md` §65.

### WP-0.9 — Deliberation capture

**Done when:** consequential exchanges are classified per `02-partner-systems.md` §4.8, a blind position is formed before exposure to preference, and the record populates.

**Verified by:**
- **Classifier accuracy:** across fifty real exchanges hand-labelled by Lord Armand, hard exclusions are never classified consequential (zero tolerance — these are unambiguous), and disagreements on the inclusion test are reviewed and used to tune.
- **Ordering is structural, not asserted.** Log the exact payload of the blind position call and confirm by inspection that no preference-bearing content is present. A test case where preference and question are in one message must show the preference absent from the blind call and present in `stripped_content`.
- Where preference cannot be separated, `ordering = contaminated` and the record is not treated as independent.
- `outcome` populates correctly across all four values in real use.
- **The disagreement signal:** time since Val last disagreed is queryable and correct.
- **Trap questions — amendment, 15 August 2026.** The WP-0.7 trap-question suite runs against deliberation records too: enthusiasm recorded in a deliberation — or an `agreed_from_start` outcome — is never reported as an approval.

> **Amendment — 3 September 2026, Lord Armand. The classifier contract failed in real use, and unknown classification is not ordinary classification.**
>
> **What was found.** Through 3 September, `blind_positions` and `deliberations` held zero rows and no strip or blind-position call had ever been made: the §4.8 classifier had never delivered a parseable verdict on a real exchange that warranted one. Of 27 classification calls on the record, six ended at the 256-token output cap and at least ten more completed unparseably. Controlled reproduction on the live route showed the shape: the exchange was sent as a bare `user` turn under an instruction-only JSON contract, and the model treated it as a message *to* it — a correct verdict followed by prose answering the exchange until the cap, or prose followed by a fenced verdict. The output cap was a symptom, not the cause; raising it was ruled out. The verdicts, where present, agreed with §4.8's test.
>
> **The repair, smallest correct surface.** Two halves. The reply is **schema-constrained** through the providers' structured-output mechanisms (Anthropic `output_config.format`, GA on the classifier's route, no extra billing beyond the format's own system text, grammar compiled once and cached provider-side; OpenAI strict `json_schema` on the fallback route) — the schema is the parser's exact vocabulary, so the parser stays strict. And the exchange is **framed as data** in a serialised envelope (`VAL-CLASSIFY-V1`), the same device as the memory envelope, so it cannot read as addressed to the classifier. The strip and blind-position contracts are unchanged; they were exercised for the first time in the demonstration below and behaved. A `GatewayRequest` may now carry `output_schema`; an adapter that cannot enforce one refuses rather than drops it.
>
> **The ruling.** Until now a classifier failure was a logged capture miss and the turn proceeded as an ordinary turn — the mechanism deciding whether the safeguard applies switched the safeguard off when it failed. **Unknown classification may not be treated as ordinary classification.** The recovery is bounded: one retry of the identical request; if the classification is still unestablished, the turn ends unanswered — the message stays in the record, no `val` message is written, every classification call is on `model_calls`, and the interface says no provider was asked for a response. It does not proceed as ordinary. Retroactive marking remains. This is not a conflict with invariant 25 (degrade rather than halt): a turn ending unanswered with a plain statement is the existing `UnansweredTurn` shape a provider outage already produces, and Val continues; what is refused is the silent substitution of a lesser path.
>
> **The record, corrected without erasure.** The 19 August report to Lord Armand described the WP-0.9 implementation as *demonstrated* on the strength of the orchestration tests, which run against a scripted provider. That was accurate about the orchestration and inaccurate as a claim about the mechanism: nothing had run against a real provider, and the first real exchanges found the contract failing. The 19 August report stands as written; this amendment records that its "demonstrated" did not cover the provider contract, and that the first real-provider demonstration of the full path is the one of 3 September 2026. The gate consequence is recorded in §5.
>
> **Follow-up rulings, 3 September 2026, on the repair's acceptance.**
>
> *Classification evidence.* A separate durable per-turn record, `classifications` (§2.2), answers how each turn was classified and why — ordinary turns included — from the classifier's declared structured reason. `model_calls` stays non-conversation; the 18 August provenance rule is intact.
>
> *The strip must not paraphrase — a defect, not sample quality.* The first real demonstration showed the strip returning a rewritten question that leaked no preference. Overruled as review material: the contract requires the verbatim remainder, because a stripper permitted to rewrite can alter emphasis, drop qualifiers, introduce interpretation, or narrow the choice, and the blind position would then answer something subtly different from what was asked — a structural guarantee, not a sample-quality issue. The enforcement is mechanical: the strip names the spans it removed, each copied verbatim; the house **derives** the blind question from the original message and those spans (whitespace-normalised, deleted in order of appearance, no overlap), refuses any span it cannot find verbatim (recorded `contaminated`), and discards the strip's own `question`, which is advisory only and logged when it differs. No semantic model judgment decides whether a paraphrase is close enough. Demonstrated the same day against the real route.
>
> *The `updated` outcome.* Inspected on the demonstration of record and reported for ruling; not changed. See the report of 3 September 2026.
>
> **Rulings of 7 September 2026, Lord Armand, closing the follow-up.**
>
> *Strip occurrence identity.* The derivation of 3 September located each removed span by its first occurrence, which is a guess when identical text occurs more than once and only one occurrence is preference-bearing. Each span now carries an `occurrence` (1-based, counted over non-overlapping occurrences of the whitespace-normalised text from the start of the message); the house removes exactly that occurrence and refuses an occurrence the message does not have, two spans claiming the same text, or overlapping spans. No semantic judgment resolves the ambiguity; the locator is the strip's, validated mechanically.
>
> *The `updated` outcome — a record-semantic defect, repaired.* The governing rule: **a user-authored claim about Val's prior position is not evidence that she held it; for the current consequential exchange, the durable blind-position record is the sole authoritative prior.** `updated` means the final position differs from the *recorded* blind position because of the argument, never that she moved from a position merely attributed to her; `agreed_from_start` means the recorded position already agreed with the stated preference and the final position remained in agreement. The reconciliation envelope now says so, and the verdict is checked structurally against the record: it must echo the recorded position (whitespace aside), declare whether the final position changed from it and whether it agreed with the stated preference, and the outcome must be consistent with those declarations; an `updated` whose final position is the recorded position verbatim is refused. An inconsistent verdict enters no outcome — the turn settles, the blind evidence stands, and the exchange resolves manually — never an invented one. No semantic-equivalence engine: differently worded positions are never judged the same by the house. The demonstration row that exposed the defect is preserved as captured, under `docs/reviews/evidence/`.
>
> *Migration isolation — a tooling defect, not a reason to reopen `0013`.* The Alembic environment now resolves its target explicitly and fails closed: a non-scratch target is refused unless the invocation is `-x deploy=live`, whatever the surrounding environment holds. Deployment is `uv run alembic -x deploy=live upgrade head` (`docs/BUILD.md`).
>
> **Residual blind-framing rule — 7 September 2026, Lord Armand, the final WP-0.9 repair before evidence resumes.** In the false-attribution demonstration the explicit attribution and preference were stripped, but dependent framing survived — "defend the close-up or change your mind" — which presupposes a prior position for Val, and the blind call answered from it. The response stage declining to emit a verdict was good behaviour and is not sufficient: the blind stage must prevent what the response stage noticed. **A blind position may not be formed on a question whose framing presupposes an unverified prior position for Val.** A user-authored claim about her prior position is untrusted unless authoritative history establishes it; framing whose meaning depends on that attribution may not reach an `ordering = enforced` blind call. The enforcement is the narrowest available: the strip's spans carry a `kind` — `preference`, or `attributed_prior` for the attribution and any framing that depends on it — and both kinds are removed by the same verbatim span-and-occurrence machinery; where removing them would require paraphrase, reconstruction, or an invented replacement question, the strip says inseparable and the position is recorded `contaminated`. The blind-payload log now also lists what was withheld and of which kind, so the evidence shows what reached the call and what did not. *Reading recorded for "where the contract legitimately permits its use":* the blind call receives the stripped question only and no history (§4.1 step 2), so a prior position never legitimately reaches it, real or attributed; it legitimately reaches the **response** call through conversation history, where the envelope tells Val an attributed prior is trusted only if it appears in her own earlier messages. Removing attributed framing from the blind question therefore never declares a real prior invented — it keeps every prior out of the one call that is defined to have none. The contract is not broadened beyond this failure shape.
>
> **Governing reading on prior positions — ruled 7 September 2026, Lord Armand.** The reading above is confirmed and is now the baseline's rule, recorded here so that nobody later "helps" by injecting a real prior into an enforced blind call:
>
> - **No prior position ever legitimately enters an `ordering = enforced` blind call.** The enforced blind stage receives the stripped current question and no conversation history. Its purpose is to establish Val's independent position on *this* question, not continuity with her previous views.
> - Claims or framing in the current message about what she previously believed are **not authoritative blind-stage input, whether true or false**. They are mechanically removed where possible. Where they cannot be separated without rewriting the question, the exchange takes the contaminated or inseparable path.
> - On that contaminated path, unsupported prior framing may remain in the whole-message payload because separation could not be established. That is permitted **only** because the resulting position explicitly does not claim independence — `ordering = contaminated` — and it may not count as evidence of enforced ordering.
> - A genuine prior position becomes relevant only at the **response and reconciliation stage**, where authoritative conversation history is available. There she may rely on something she actually said in her own earlier messages. Lord Armand's assertion that she previously held a position is not evidence by itself.
>
> **Contaminated rows are not evidence of enforcement.** A contaminated blind-position record never counts as evidence that ordering was enforced merely because a blind row exists. It is valid operational evidence of the contaminated path. Any gate criterion requiring independent or enforced ordering — point 5's "ordering enforced" — uses rows that actually record `ordering = enforced`.
>
> **The partial live demonstration is sufficient.** The mechanically-removable `attributed_prior` branch is demonstrated structurally and by regression tests; the live route demonstrated the conservative failure — when separation was uncertain it chose inseparable, recorded contamination, and did not claim enforced independence. The unsafe failure, claiming `enforced` while seeded framing remained, did not occur. The mechanical-removal branch is captured when real use produces it; no turn is manufactured to force it.
>
> **The logger defect** (alembic's `fileConfig` disabling existing loggers, so blind-payload evidence lines were absent in any test process that migrated first) is closed with the repair and its regression coverage. The finding is preserved here; completed work is not reopened solely because historical evidence lines may have been absent in affected test processes.
>
> **The fifty, made recordable — 7 September 2026.** The classifier-accuracy criterion had no mechanism: the interface showed no verdict and nothing could hold Lord Armand's label. Ruled and built: an in-app review queue, blind before reveal, with the original label immutable, the hard-exclusion determination explicit, and the scoring of amendment 3 — recorded in §2.2 under `classification_labels` and `classification_reviews`. Eligible exchanges accumulate from the resume boundary whether or not the interface existed when they were classified; nothing is manufactured.
>
> **Resume — 7 September 2026.** Every condition set for the WP-0.9 follow-up is closed with CI green on b6d5c32 and the repaired service deployed. Normal creative work with Val and consequential gate-evidence collection resume. Point 5 and the fifty hand-labelled exchanges begin from zero; nothing from the paused interval counts; no interaction is manufactured to accelerate either. The live-route attributed-prior removal case, consequential latency, and other operational behaviour accumulate through use. No further WP-0.9 repair cycle absent a new concrete invariant or contract violation.

> **Amendment — 8 September 2026, Lord Armand. The blind position's ceiling, and its bounded retry.** The quality-floor demonstration of 7 September found the blind call on the partner route exceeding its 1,024-token output ceiling — calibrated when the cheapest route served it — and, under the fragment doctrine, the orchestrator writing no position and **proceeding as an ordinary turn**: a consequential exchange answered without a recorded position, silently. Ruled: the blind-position maximum output is **4,096 tokens**, matching the response allowance — an output ceiling, not a request for a long position. **A truncated or otherwise invalid blind position may never cause a consequential exchange to proceed as an ordinary turn.** Governing behaviour: a valid position continues normally; an invalid or truncated one is retried **once**, on the exact same configuration, schema, stripped blind input and ceiling; a second failure ends the turn **honestly unanswered** — the user message preserved, no Val response, no fabricated position or deliberation, both attempts preserved in `model_calls` under their own terminal states and named in the surfaced cause. No fallback to the ordinary path. Regression coverage: truncation, completed-but-invalid structured output, retry success, retry failure.

> **Amendment — 9 September 2026, Lord Armand. The strip invariant: `ordering = enforced` requires deterministic proof that preference-bearing material was actually removed.** The strip conformance suite of 8 September (fourteen frozen synthetic cases, 448 real calls through four structured routes) found the registered strip route recording a genuinely inseparable message as `separable: true` with no spans — after which the orchestrator derived a "blind" question identical to the original and recorded the blind position as enforced. That is an independence violation wearing the enforced label. Ruled and built: a structured strip result is validated deterministically against the current message before anything is built from it, and **enforced ordering exists only when all of these hold** — `preference_present = true`, `separable = true`, one or more valid `preference` spans, an `attributed_prior` span wherever one is declared, every span resolving exactly against the submitted message at its occurrence, the derivation actually altering the message, and the blind payload equal to that derivation. `preference_present = false` can carry no removal spans and can never produce enforced ordering; `separable = false` can carry no spans and never yields an enforced payload. **Any internally contradictory structured result is invalid**: it is retried exactly once on the same exact configuration; a valid "not separable" is never retried in search of separability; a second invalid result fails closed as contaminated under the existing honest semantics; an enforced blind row is never manufactured. Every attempt's validation state is recorded on the outcome. Regression: the S7 contradiction and the symmetric invalid states, including `preference_present = false` carrying spans (`packages/policy/tests/test_strip_invariant.py`, `packages/gateway/tests/test_strip_invariant_orchestration.py`). **The durable rule:** when a preference is reported present and successfully removed, deterministic code must be able to prove that valid preference-bearing material was actually removed before `ordering = enforced` can exist. That is the boundary between model judgment and machine-enforced evidence.
>
> **The strip contract's boundary is confirmed, not widened (same ruling):** the strip removes the *author's* preference and attributed priors. A third party's recommendation — "Casting says keep.", a counsel's or an engineer's view — is substantive information about the decision and is **retained** in the blind input; the blind position is independent of Lord Armand's stated preference, not ignorant of stakeholder recommendations. The contract is not amended to strip anyone else's view.
>
> **The strip is its own capability floor (same ruling; `01-architecture.md` §5.2):** `CapabilityProfile.STRIP`, required by the strip task and held only by configurations that have demonstrated the strip contract on the frozen conformance suite — zero preference leakage into an enforced blind payload, zero unsupported attributed-prior leakage where removal is required, zero neutral-decision-content removal, zero genuinely inseparable cases reaching enforced ordering, exact expected residue on every separable case (allowing only recorded punctuation alternatives), and no false contamination on clearly separable cases. Claude Haiku 4.5 holds it only until a demonstrated successor is designated; its record on the suite (35 of 112 conformant, 27 false contaminations, 50 blocking, removal of neutral instructions such as "Decide and defend it.") is the reason the floor exists. **Designated — 9 September 2026, Lord Armand, on the pre-authorised path:** suite v2 (the corrected ground truth) through the real gateway with the invariant, eight runs per case, pinned to the exact registered configuration — `gpt-5-5-20260423` **112 of 112** conformant, no false contamination, no invalid attempt, no retry; `sonnet-5` **110 of 112**, zero blocking, zero false contamination on separable cases, no invalid attempt, and on 2 of 8 runs the genuinely inseparable case returned as "no preference" (an ordinary turn, never an enforced row — recorded as an operational finding on the entry). Both meet the bar and both are designated **strip-eligible only**; routing chooses between them by total call cost (Sonnet 5 leads; GPT-5.5 is its declared, independently re-checked successor). **Claude Haiku 4.5 is removed from strip eligibility** and keeps `structured` for classification and titling until those are separately evaluated (OP-4). Records: `docs/reviews/qualification/strip-conformance/v2/`. Holding `strip` confers nothing for classification, titling, conversation, or the partner calls; passing another structured task confers nothing for the strip.

> **Amendment — 10 September 2026, Lord Armand. The strip cost/latency correction.** The live turn of 18:25 (a 1,269-character correction quoting Val, classified consequential) ran the registered strip route (`sonnet-5`, effort `high`) to its 4,096-token output ceiling twice — the ruled identical retry reproduced the first truncation — then made a blind-position call on the whole message under `contaminated`: 81.7 s and $0.088 for nothing, and a further partner call to record a position formed with the preference in view (`docs/reviews/VAL_Strip_Route_Finding.md`). Ruled and built: **(1) Option A — a strip attempt whose provider terminal state is `truncated` receives no identical retry.** The terminal state is the provider's own and is on the `model_calls` row; an identical request against the same ceiling is the same failure bought twice. The one bounded retry of 9 September is preserved only for a *completed* response whose structured result is invalid or unparseable. The strip output ceiling is not raised. **(2) A strip that cannot establish an enforceable blind payload — truncated, failed, invalid after its bounded retry, or a valid `separable = false` — makes no blind-position call.** The strip/capture failure is recorded honestly (every attempt's state on the outcome, every call on `model_calls`, the classification record intact), **no `blind_positions` row is written, no deliberation outcome is fabricated**, and the turn collapses to the ordinary partner-response path — the same shape as the `no_preference` collapse. A position formed with the preference in view is the thing the machinery exists to avoid; paying a partner call to record one was spending on the failure mode. `ordering = contaminated` remains in the domain for the rows written before this date, which stand as what they were; the live path no longer writes it. Regression: a truncated strip causes exactly one strip provider call and no blind call; a failed call, an exhausted invalid result and a valid inseparable each make no blind call and write no row; an enforceable strip still forms the blind position, durable, before the response; `preference_present = false` still collapses; historical contaminated rows remain readable (`test_deliberation_machinery.py`, `test_strip_invariant_orchestration.py`). **(3) The route question, staged, no switch.** S15 and S16 (the long-correction shape, synthetic) were approved as screening cases; the broad matrix was not spent. Candidates were registered **for evaluation only** — `NOT_ADMITTED`, no capability profile, absent from `active()` and so from routing, cost ranking and liveness, reachable only through `Gateway.evaluate_with_configuration`, a door narrower than the pinned path (schema-constrained structured work only, never conversation or the blind position, never an admitted configuration, never a caller's copy, eligibility checked as for any call); designation is a recorded ruling that edits the entry, never a consequence of passing. Registered: `sonnet-5-medium`, `sonnet-5-low`, `gpt-5-6-terra` and `gpt-5-6-luna` at effort `none` (`ReasoningEffort.NONE`, OpenAI's documented level for latency-critical work; the Anthropic adapter refuses it rather than substituting). **Screen result (`strip-conformance/v3/screen-2026-09-10.md`, $0.62 in all, figures stated before each stage):** Sonnet at `medium` and `low` — no truncation, no retry, S16 exact on 4 of 4, 6–10 s; Terra — no conformant result, three `invalid` (an attributed prior declared without its span); Luna — removed decision content; GPT-5.5 (permitted once every candidate had failed) — truncated on 3 of 4 at ~44 s and $0.127 a call, reproducing the live failure class. **Every configuration failed S15 as drafted, four of five identically**, by removing "I want you to preserve that distinction going forward …" and "I do not want you to turn that into invented lore. I want us to define it carefully …" as preference — which the contract's own rule ("when in doubt about a clause, remove it") reads as removable and the drafted ground truth reads as conduct instructions, not preferences on the question asked. That conflict, and whether a verbatim quotation of Val's earlier words offered to correct a fact is an `attributed_prior`, are **returned for ruling** (exam-repair principle); suite v3 is not frozen, no candidate is designated, no route is switched, and the registered strip routes stand (`sonnet-5` at `high`, fallback `gpt-5-5-20260423`, each now carrying the observed truncation on its entry). Under part 4 nothing further was spent.

> **Amendment — 11 September 2026, Lord Armand. The three strip questions ruled; the strip contract amended.** **(1) Conduct directives are retained.** An instruction about *how* to answer — form, process, length, order — is not a preference between alternatives, including when phrased as a want: "decide and defend it", "give me your own recommendation first", "keep it under two hundred words", "I want you to preserve that distinction", "I do not want you to invent lore" are all retained. The strip removes material that biases *what* Val should conclude; a directive about how to work does not. **(2) A new retained category, `assistant_record_evidence`, distinct from `assistant_prior_position`.** Val's exact earlier words quoted as evidence of what was said — where the task is to identify, verify, correct, explain or discuss the utterance itself — are retained; Val's earlier substantive view offered as authority for what she should believe now ("you previously thought X, therefore preserve X") is an attributed prior and is stripped. The governing distinction: strip prior material that biases what Val should conclude; retain record evidence necessary to establish what Val actually said or did. Conditions: the quotation must be grounded in retained history (an alleged quotation that cannot be verified against the record never silently becomes trusted evidence); only the material needed for the referent is retained; a user preference wearing quotation marks is still stripped — classify by attribution and function, never by punctuation. **The mixed case is closed explicitly:** a quotation carrying both record evidence and a substantive prior conclusion ("You said, 'Scene 4 should be deleted because Joni has no motivation.' Reconsider the scene from scratch.") has the conclusion and its reasoning stripped; the exception is never a route for feeding Val her own old answer before an independent judgment. **(4) The truncation incident does not reverse Sonnet's qualification**, and the completeness guard is explicit in the contract: an incomplete or truncated strip fails closed and never enters the blind-position record.
>
> **Built (`val_policy.deliberation`, `val_gateway.deliberate`):** the strip instruction states (1) and (2) in the model's terms; the strip result gains a required `record_evidence` list — each retained quotation of Val's words, verbatim, with its occurrence — and the house checks every declared quotation deterministically: it must be found verbatim in the current message at its occurrence and overlap no removed span (either failing is `invalid` — a quotation is retained as evidence or removed as a prior, never both, which is the deterministic half of the mixed-case rule), and it must be **grounded** in the conversation's own record of what Val said (the content of her messages in the authoritative store, matched literally, whitespace, letter case and terminal punctuation aside), failing which the result is `ungrounded` — final, never retried, never enforceable, no blind call. `validate_strip` carries the **completeness guard**: a reply whose provider terminal state is not `complete` is `truncated` or `incomplete` before any parse of the fragment is trusted. An attributed prior with **no** author preference is now expressible and enforceable (`preference_present = false`, `attributed_prior_present = true`, attributed spans listed) — the 7 September rule had no representation for it, so the mixed case could not have been stripped at all. **The stated limitation, reported rather than covered:** whether a *grounded* quotation is record evidence or a prior conclusion is the model's judgment; no deterministic rule distinguishes "you greeted me with 'good evening'" from "you said 'delete scene 4'" by text alone. The house enforces grounding, verbatim resolution, non-overlap, and that nothing declared removed survives in the payload; and where the task identifier and the conclusion share one clause ("Scene 4 should be deleted because…"), the verbatim mechanism cannot keep the identifier without rewriting, so a conformant strip either removes the whole attributed sentence or declares `separable = false` — in both the prior never enters a blind payload, at the cost of an under-identified or absent blind position on that exact shape. **Suite v4** (`strip-conformance/v4/`, frozen 11 September 2026 before any run): v3 with S15/S16 ground truth amended under (1) and (2) — the quoted Val sentence declared as record evidence, grounded in the case's own `history_val_messages` — plus **S17**, the mixed case, accepting either the whole-sentence attributed removal or a valid inseparable, and BLOCKING on the conclusion surviving anywhere. v3 and every prior result are unchanged. Regressions: `test_strip_invariant.py` (guard, grounding, overlap, ungrounded, attributed-only) and `test_strip_invariant_orchestration.py` (grounded quotation reaches the enforced blind call in a conversation where Val said it; ungrounded is final with no blind call; attributed-only enforced; a quotation both retained and removed never reaches a blind call).
>
> **(3) The screen resumed on Sonnet only, and the full frozen v4 (`strip-conformance/v4/results-2026-09-11.md`; $0.9858 measured in all, each stage stated first and under a dollar).** Screen, S15/S16 × 2: `sonnet-5-medium` 4/4 (12.1 s median), `sonnet-5-low` 4/4 (7.0 s); no truncation, no retry; leader by latency `sonnet-5-low`. Full v4 on the leader: **134 of 136**, median 2.6 s, $0.3881 — S15 r5 retained the quotation without declaring it (no payload, nothing leaked; the v4 declaration requirement only), and S17 r7 removed the quoted conclusion but left "You said," dangling (exact residue on a separable case, an enumerated floor criterion; the conclusion never survived). Next survivor, `sonnet-5-medium`: **133 of 136**, median 3.1 s, $0.5019 — C5 r4 removed the third party's recommendation with the preference (neutral decision content, a floor failure), S7 r2 the known inseparable-as-no-preference finding, S17 r5 the same residue shape. **No configuration passed the entire frozen v4 on the floor's own text, so none is designated by the implementer**; the registered strip routes stand (`sonnet-5` at `high`, fallback `gpt-5-5-20260423`, the amended contract deployed on them under (1), (2) and (4) — the guards only ever fail closed, and `sonnet-5` at `high` was not itself run on v4). **Returned for ruling:** whether `sonnet-5-low` — every miss non-leaking, the one floor miss being the mixed-case residue on one run in eight, the shape whose limitation is stated above — is designated under the same operational-finding treatment the 9 September designation gave `sonnet-5`'s S7 finding, or whether the S17 residue disqualifies it. The strip work is not closed, and the state is not tagged, until that ruling.

> **Ruling — 11 September 2026, Lord Armand. Strip route designation: `sonnet-5-low` holds `strip`.** An **owner-authorised operational designation with a recorded residual finding, not a declaration that frozen v4 formally passed.** The frozen v4 result stands exactly as measured, **134 of 136**; the suite, floor, judge and historical results are not altered. The residual finding, recorded on the entry: S15 r5 retained the grounded quotation without declaring it as record evidence — no blind payload was formed and nothing preference-bearing leaked; S17 r7 removed the substantive prior conclusion but left the inexact residue "You said, Reconsider the scene from scratch." — the conclusion did not survive into a blind payload, and exact residue remains a formal floor miss; the mixed-case limitation remains an acknowledged limitation of model judgment, not hidden behind a deterministic heuristic and with the contract not weakened. The operational basis: no preference or substantive-prior leakage into an enforced blind payload across the full run; every observed failure fails away from false independence; no truncation at low effort; 134 of 136; median 2.6 s, maximum 11.5 s; $0.3881 for the suite; the deterministic completeness, grounding, overlap and no-blind guards in force. **`strip` is removed from `sonnet-5` at `high`** so routing cannot tie with the new route; its v2 result and truncation history are preserved as historical evidence. Terra, Luna, GPT-5.5, medium and high are not rerun; `gpt-5-5-20260423` keeps its fallback standing unchanged (no mechanical registry constraint required a change — the chain `sonnet-5-low → gpt-5-5-20260423 → none` terminates). The existing regression suite ran unchanged and CI is green; the state is frozen and tagged `strip-closed-2026-09-11`. **The strip work is closed.** No further strip qualification, correction or general audit; the Vale Core provider-neutral refactor is not begun in this change.

### WP-0.10 — Text interface

**Done when:** the interface supports daily use without developer tooling.

**Verified by:**
- A full day of real work conducted entirely through it, with no terminal, no database client, no log tailing.
- Project switching, conversation history, and marking an exchange consequential are all reachable from the interface.
- Nothing in the interface displays a state the database does not support (`00-charter.md` invariant 29).

> **Amendment — 7 September 2026, Lord Armand. Project creation from the interface.** The operating briefing of this date found that the only live conversation was explicitly no-project, that every project in the store was an archived demonstration fixture, and that the interface could select projects but not create one — an operational hole next to "conducted through the interface, with no developer tooling." Ruled: the smallest proper user-facing creation path is built — **New project, name, create, select** — rather than inserting the first real project through developer tooling. The slug is derived mechanically from the name; status is `active`; the description is empty; a name or slug held by an existing project, archived or not, is refused in words rather than suffixed, because an archived project still resolves by name and a second would manufacture ambiguity. Nothing beyond creation and selection is authorized; this is not a project-management feature. The first real project is **Tony Spumoni**. Ambiguity evidence for the gate continues to rest on the August demonstration until it arises naturally; no project is created to manufacture it.

<!-- scope-ruling: 2026-09-02 -->
### WP-0.11 — Budget-control hardening (warn-and-raise) — recorded 2 September 2026, not yet begun

> **Status, 10 September 2026, Lord Armand.** The qualification-dependent hold on this work package is released with the closure of the qualification repair loop (`01-architecture.md` §5.2, ruling of this date): `opus-5 / medium / adaptive` under persona v1.4 is in substantive operational service under the owner-authorised operational exception (formal status *not met*, one known residual integrity defect, OP-5). WP-0.11 proceeds in its recorded place under the existing roadmap; nothing in its scope changes.

**Ruled by Lord Armand after both external reviewers converged. Its own work package, not Track C**: Track C's isolation rule is attachments and derived views only, and budget policy colliding with vision in practice does not pull it into the substrate. Layer 0, because the ceiling it hardens is Layer 0's (`01-architecture.md` §5.5 as ruled 2 September 2026).

**What it blocks and what it does not.** It does **not** block the attachment migration or the internal vision implementation after the storage ruling; those proceed on their existing path. It **does** block Track C becoming an operational working-day capability, and it blocks visual conversations contributing evidence to the Layer 0 gate — otherwise "she cannot see" is traded for "she stops mid-session and the raise is a code edit," the same stall through a different door. **Not a development blocker. A live-use blocker.**

**Minimum shape when this work begins:**
- Operating target and runaway safety ceiling are separate concepts with separate values. Neither is a source-code literal.
- The operating target never admits, refuses, or reroutes. It is planning and reporting information only. **Only the runaway ceiling can refuse a call.**
- A clear, non-blocking warning before the ceiling is reached; the first indication of approaching the limit may not be a hard refusal. The initial threshold is proposed with the work-package design, not ruled now.
- Lord Armand can raise the safety ceiling at runtime without editing code, changing schema, restarting the service, or rebuilding Val. The raise is durably attributable: amount, scope, time, actor.
- A runtime raise changes the safety ceiling, not the operating target, and never retroactively alters historical reporting; the concepts stay separate through the records and the interface.
- Reservation, accounting, cost capture, and hard-refusal behavior remain authoritative against the effective ceiling.
- **The degrade behavior is fixed here.** Today a routed call's `BUDGET_EXCEEDED` is retryable, so a turn steps down to a cheaper eligible route and refuses only when nothing affordable remains — Val silently doing worse work to stay under the only number she has, contradicting the quality priority and reading as resilience, which is why it went unnoticed. Under this package: a ceiling hit refuses, or resolves to `UnansweredTurn`. It does not pick a lesser model. An operating-target hit does nothing to the call at all.
- **The number is Lord Armand's.** The refusal must not remain at $200 under a new name — that is still a planning figure stopping a heavy day. The safety ceiling is accident-scale: a figure that trips only when something is genuinely wrong, never because he is working hard. The design proposes the shape; the value is his.

**Not authorized:** automatic increases, forecasting, dashboards, provider-specific budget management, spend optimization, or a policy engine. Premature.

> **Values ruled — 7 September 2026, Lord Armand**, so the package does not stall when it begins: **operating target** approximately $250 per month, an annualized planning average, reporting only and never an admission limit; **warning threshold** $800; **runaway safety ceiling** $1,000 per month. The $1,000 is an accident-control tripwire, not a spending target and not a capability limit; it must never cause silent quality degradation; at the ceiling the call is refused honestly under the governing ruling, and the authorized runtime raise records amount, scope, time, and actor. The old $200 source literal is neither reused nor reinterpreted.

### Migration item — a structured-route successor before Claude Haiku 4.5 retires — recorded 8 September 2026

**Ruled by Lord Armand.** Classification, strip, and title depend on the `structured` profile, and Claude Haiku 4.5 (`haiku-4-5-20251001`) carries a retirement "not sooner than October 15, 2026". **Before that window**, a current configuration is demonstrated under the same classifier, strip, and blind-position schemas in the scratch store and designated the structured route by ruling. Existing OpenAI fallback evidence may contribute but does not substitute for demonstrating the replacement. No partner qualification is needed to serve the structured profile. This item blocks neither the empty-response repair nor the economics work. Tracked as `docs/reviews/VAL_Open_Problems.md` OP-4.

> **Amendment — 31 August 2026, Lord Armand. Invariant 29 applies to error display.** An error message that names a cause it has not established is a false claim. Recorded from real use: the interface asserted the service was "not reachable" while the service was demonstrably healthy — the actual failure was browser policy — and the asserted cause sent diagnosis down a network path that did not exist. Binding on the interface: a failure is reported as what was actually observed ("no response", "the service refused with this status", "the reply could not be read"), never as a diagnosis the observing code cannot make; where script cannot distinguish two causes, the message names both rather than picking one; and a multi-step operation reports each step's failure individually rather than collapsing them into one claim.

> **Amendment — 11 September 2026, Lord Armand. The unassigned-chat interaction model, and the stream in the interface.** Opening Val places him directly in a new, unassigned conversation; New chat does the same; no project selection is required for ordinary conversation, and "No project" does not exist as a user-facing option — neither as a sidebar filter nor as a selector, and no replacement selector such as "Unassigned" is required: unassigned is the absence of project scope, not a choice. A conversation becomes project-scoped only when he intentionally enters a project (the sidebar) and begins a conversation from within it; the previously entered project never carries into a new chat implicitly; existing project-attributed conversations keep their attribution, since a continued conversation sends only its id and the record decides. The service contract is unchanged — the desktop sends the existing explicit `no_project: true` for an unassigned conversation, `project` for an entered one — and no schema change or migration renames the null state (`apps/desktop/src/scope.ts`, tested). **The stream reaches the interface** (responsiveness phase; `01-architecture.md` §5.1 ruling of this date): `POST /turns/stream` delivers Val's generated text as server-sent events, every delta having passed through Val Core's sink and the orchestrator's verdict filter, and ends with the identical settled object `POST /turns` returns plus timing measured at the gateway and the service; the desktop shows her words as they arrive, marked in progress, replaces them with the settled and persisted message, records the frame on which her first words were painted, and reports the last turn's timing by where each figure was measured. The plain route is unchanged. Evidence and the open user-visible acceptance: `docs/reviews/VAL_Responsiveness_Phase_Record.md`.
>
> **Amendment — 13 September 2026, Lord Armand. Stage progress, and timing labelled by clock origin.** **Progress:** a streamed turn whose request asks for it (`progress: true`, which the desktop sends; the stream a client did not ask to change is unchanged) receives `stage` events at the moment the backend can truthfully establish each stage — `understanding` when the message is persisted and the turn begins, `forming_view` when the enforced blind-position call is about to be made (never on a turn that forms none), `preparing_response` when the final response call is about to be made. Events carry only the stage and the service's elapsed milliseconds: no classification label, stripped preference, position or text. They are presentation state, never stored as conversation and never Val's speech, and the desktop shows them in plain words only until her first words arrive. **Progress feedback does not count toward the 1–2 second generated-text requirement; it makes a necessary wait legible and does not satisfy responsiveness acceptance.** **Timing:** the settled timing gains `api_response_started_ms` (the service's receipt → the final response call beginning). The desktop measures every client figure from Send — work before Val began answering, first text received, first render, confirmed first paint (the second animation frame after the render that showed her text), complete — and labels the response call's own time to first token separately, from that call's start, never beside them as if comparable. It records whether the window was visible at the first delta, render and paint, and every visibility change during the turn, shows a visibility line when the window was hidden, and writes the whole record to the developer console; nothing is persisted. The 10.14-second delta-to-paint gap of 13 September remains undiagnosed until this instrumentation observes one.

---

## 4. The blind position call at Layer 0 — a deviation

`02-partner-systems.md` §4.1 routes the strip step to local inference. **Local inference does not exist until Layer 1.**

> *Tripwired, 1 September 2026: this deviation carries its own expiry — when a local inference route exists in the Model Configuration Registry, `infrastructure/ci/check_scope_ruling.py` fails until this section moves. Temporary deviations with built-in invalidation conditions are exactly the rules that become permanent by accident.*
>
> *Expiry condition made precise — 16 September 2026, Lord Armand.* The first on-device provider (`lmstudio`, §5 amendment of this date) is registered **for evaluation only** — `NOT_ADMITTED`, no capability profile, conversation-only — and nothing routes to it. A route the strip could move to is one **admitted to routing**; an evaluation-only entry is not, so this deviation stands, and the tripwire consults the registry's admitted routes rather than the presence of a provider string. The strip route designation of 11 September 2026 is unchanged. When a local route is admitted, this section moves as the 1 September tripwire intended.

<!-- scope-ruling: 2026-09-21 -->
> **The condition arrived, and the deviation is closed — 21 September 2026, Lord Armand.** A local Partner route is admitted to routing (`gpt-oss-20b-mxfp4-mlx-lmstudio-partner`, §5 amendment of this date), so the tripwire fired exactly as the 1 September ruling intended and this section has moved. **The strip does not move with it.** The owner ruled in the same instruction that consequentiality classification and preference stripping remain on their existing structured cloud routes for that pass: they were not redesigned, not localised, and not examined. The deviation marker is therefore removed — not because the strip became local, but because "until local inference exists" is no longer the reason it is not. The reason now is an explicit decision, and a decision is reviewable in a way a lapsed condition is not.
>
> **What this is not.** It is not a permanent architectural exception to the local-first direction, and the owner said so in the words of the ruling: the support calls are *existing support infrastructure left unchanged for now*. Localising them is later work with its own ruling, and until then a consequential turn spends real money on those two calls while Val's own two calls cost nothing. Nothing here may be cited as settling where the strip belongs.

Layer 0 therefore runs the strip step on **the cheapest configured route** — which, under §1.1, is Protected-eligible by construction — and moves it to local at Layer 1. This is a routing deviation, recorded here rather than left to be discovered.

The alternative — recording positions at Layer 0 without enforcing ordering, and adding the structure at Layer 3 — was rejected. Positions recorded without the ordering guarantee are contaminated by construction, and they would seed the prediction ledger with exactly the data the mechanism exists to exclude. Months of records that cannot be trusted are worse than no records, because they look like evidence.

Cost: two extra calls on consequential exchanges only, one of them on the cheapest configured route. At Layer 0 conversation volumes this is small, and it is measurable from day one via `model_calls.task_type`.

> **Amendment — 19 August 2026, Lord Armand. The true cost, stated.** The paragraph above prices only the deliberation structure. The §4.8 classification is itself a model call, and it runs on **every** exchange, before any position is formed, because it decides whether the structure runs at all. The true recurring cost is therefore: **one classification call per exchange on the cheapest eligible route, plus the two calls above on consequential exchanges only.** This cost is approved. Classification spend is reported **separately** in the cost view from day one — if it comes to dominate, that is read from the record, never inferred.
>
> **Resolved reading — 19 August 2026, Lord Armand.** `02-partner-systems.md` §4.8 says that at Layer 0 the classification "gates recording only," while this section runs the blind-position structure at Layer 0. This document owns Layer 0 scope and records the deviation deliberately, so the resolved reading is: *"recording only" means recording, and the calls recording requires.* The contrast §4.8 draws is with Layer 3, where the classification additionally gates machinery beyond capture.

---

## 5. The Layer 0 gate

Layer 0 is complete when all of the following hold simultaneously, demonstrated in one session:

1. A real conversation, conducted through the interface, with no developer tooling.
2. Attributed to a project, with resolution deterministic and ambiguity surfaced rather than guessed.
3. Persisted across a full restart of application and database, resuming with history intact.
4. With `execution_events` populating — acceptances, rejections, revisions, corrections, each with its reason and `reason_source`.
5. With `deliberations` populating on consequential exchanges, ordering enforced and demonstrable from the logged blind-call payload.
6. With `model_calls` populating on every call, cost and project and task type present, and zero uncosted calls.
7. **And a verified restore from backup** — restored to a scratch instance, row counts and referential integrity checked, capture tables continuous.

Points 4 through 7 are the gate. Points 1 through 3 are what makes the layer pleasant to use; 4 through 7 are what makes the next five layers possible.

**Definition of done:** every work package's acceptance criteria met and demonstrated, not asserted. Compiling is not completion. Passing unit tests alone is not completion (`00-charter.md` §4).

<!-- scope-ruling: 2026-09-03 -->
> **Gate-state ruling — 3 September 2026, Lord Armand. Point 5 restarts from zero; evidence depending on consequential deliberation is paused until the repair is demonstrated.**
>
> Point 5 had not lacked samples; the machinery it names had never executed successfully in real use (WP-0.9 amendment of this date). An unparseable classification attempt is not a classification and counts for nothing. Accordingly: **point 5 stands at zero deliberations**, and WP-0.9's fifty hand-labelled exchanges start from zero — no prior classification is on the record to label against. Points 4, 6 and 7, and the real conversations behind points 1–3, are unaffected and keep their evidence; point 6 continues to count the failed classification calls, which are costed honestly. Accumulation of point-5 evidence and of the fifty resumes only once the repaired path has been demonstrated end to end against real providers, and then through Lord Armand's real use — no manufactured exchanges. **Creative work with Val was paused by Lord Armand from 3 September until the follow-up rulings (classification evidence record, strip enforcement, the recorded-prior rule, migration isolation, the blind-framing rule) were complete; the pause was lifted on 7 September 2026 with CI green on b6d5c32. No conversation held in the paused interval is gate evidence for classification or deliberation.** Point 5's "ordering enforced" is satisfied only by `blind_positions` rows recording `ordering = enforced`; a contaminated row is evidence of the contaminated path, never of enforcement (WP-0.9 amendment, 7 September 2026).
>
> **Point-5 evidence annotation — 7 September 2026, Lord Armand, on the quality-floor ruling (WP-0.4 amendment of this date).** The first real deliberation since the restart — `blind_positions` row `01a07ec5-5649-7dcf-977b-2129ad2c5ce6` (`ordering = enforced`, 21:07:37 local) and its `deliberations` row `01a07ec5-7396-78ce-a306-a1415daf0d62` (`outcome = held`) — **stands as valid point-5 evidence of the enforced path and a held position.** It was produced before the capability floor existed, on the route the cost-ordered router selected, and is annotated accordingly: **it is not evidence that partner routing worked**, and **it may not serve as the closing session's consequential turn** — that turn must be produced after the floor is deployed and demonstrated. The contaminated pair of the same evening — `blind_positions` `01a07ec5-e17c-7380-9375-c070e9cf8d37` and `deliberations` `01a07ec6-1bf7-73a9-858f-a5a64d997b99` — remains non-evidence, as the ruling above already provides. Point 5 therefore stands at **one** enforced deliberation. Recorded in `docs/reviews/VAL_Test_and_Evidence_Index.md` §12.
>
> **Revalidated — 9 September 2026, Lord Armand, after the strip invariant.** The enforced label alone is no longer sufficient evidence, so the row relied upon was re-checked against its durable strip evidence and logged blind payload rather than its label: `blind_positions.stripped_content` holds the removed span (the author's stated intent), the stored user message minus that span reproduces exactly the question in the logged blind payload ("Start by telling me what categories this should contain."), and the payload contains none of the removed material. **The 21:07 row passes and is preserved as point-5 evidence.** The 21:08 contaminated pair remains non-evidence. No general historical audit was begun.

<!-- scope-ruling: 2026-08-31 -->
> **Sequencing ruling — 31 August 2026, Lord Armand. Two tracks.**
>
> **Track A, mandatory:** gate evidence accumulates through real use. No manufactured judgments, no deadline. The gate closes when the evidence exists.
>
> **Track B, permitted in parallel:** Layer 1 presence only, under the hard constraint stated in `01-architecture.md` §2.1 — it consumes the existing conversation contract and changes nothing about it (no new table, no new column, no migration, no change to what the conversation endpoints return); a presence feature needing any of those stops and waits for the gate.
>
> **Everything else stays behind the gate**, and the post-gate order is decided now so it is not re-litigated then:
>
> 1. Message revision and retraction (§2.1 requirement, 31 August 2026)
> 2. Attachment substrate, **with provenance designed once for all four modalities** — not per-modality as each arrives
> 3. Documents
> 4. Image vision (`01-architecture.md` §2.2 requirement, 31 August 2026)
> 5. Audio and video afterward, **each designed against specific real tasks, with a cost model established before implementation**

<!-- scope-ruling: 2026-09-01 -->
> **Amendment — 1 September 2026, Lord Armand. Sight re-scoped out of Layer 2 (`01-architecture.md` §2.2, as amended); the post-gate order changes with it.**
>
> Image vision depends only on the attachment substrate, not on documents, so items 3 and 4 above become **siblings rather than sequential**. The amended order:
>
> 1. Message revision and retraction
> 2. Attachment substrate, provenance designed once for all four modalities before either sibling begins
> 3. **Image vision and documents, as siblings** — image vision is attachment-scoped (an image deliberately attached to a conversation), which satisfies "sight, not receipt" without Layer 2 Hands; MCP filesystem access — reading folders at scale — stays Layer 2
> 4. Audio and video afterward, each designed against specific real tasks, with a cost model established before implementation

<!-- scope-ruling: 2026-09-01 -->
> **Amendment — 1 September 2026, Lord Armand, from external review. The attachment-substrate acceptance boundary.** Both reviewers found a hole in the sibling ruling that neither Lord Armand nor the implementation caught: the design-once rule did not cover **mixed-modality files**. A PDF with figures, a slide with a frame — "what she saw" and "what the document says" are *derived views of one attachment*, not two attachments, and without a rule, page 17 and the figure on page 17 become two provenances for one file.
>
> **Attachment Substrate v1 must be accepted as a shared contract for both image vision and document comprehension before implementation of either consumer begins.** The contract establishes at minimum: the immutable original; attachment identity and hash; typed derived representations; parent/child provenance; processing and model provenance; structural locators such as page/slide/region; processing status and errors; and rules for extending the representation model later.
>
> **The standard is contract-stable, not frozen.** Core provenance semantics may not be privately changed by either sibling. Additive typed representations are permitted. If a sibling finds a core assumption wrong, the contract is amended deliberately rather than quietly becoming the vision version of the attachment model.
>
> **Siblings describes dependency, not mandatory concurrency:** after the substrate is accepted, either may be implemented first, or both in parallel.

<!-- scope-ruling: 2026-09-02 -->
> **Track C — gate-enabling capability. Ruling, 2 September 2026, Lord Armand, after external review; both reviewers independently reached the same conclusion and the same narrow scope. A new track, opened against his own gate-first constraint.**
>
> **Why.** The gate-first ruling was made before Lord Armand had tried to have a working day with Val. Having tried, the finding is that the gate has a **collection-pathway failure nobody modelled**: it assumed the pre-gate product would be useful enough to generate evidence organically. His organic use is visual production work; a text-only Val cannot examine that work, so the evidence either does not accumulate or accumulates thin — her reasoning from his descriptions rather than from the thing itself, the exact inferior evidence class the sight amendment exists to prevent. **That is a defect in how evidence is collected, not evidence that the Layer 0 criteria are wrong.** Night one stands. The bar is not lowered. The sentence that decided it: **a gate that cannot be reached does not keep its authority. It gets abandoned later, messily.** Changing the sequencing assumption while preserving the evidence bar is what stops the gate becoming ceremonial.
>
> **Scope: Attachment Substrate v1 and attachment-scoped image vision. Nothing else.** Explicitly held, not opened: message revision/retraction (a real pain and a different pain — not what stalled the working day), document comprehension (the sibling that waits), audio, video, MCP filesystem, and every later-layer capability. The substrate acceptance boundary above stands in full: Substrate v1 is accepted as a shared contract for both siblings before either consumer begins, contract-stable not frozen, mixed-modality provenance intact. Vision is its first consumer; documents still do not start. **Track B is unaffected**: Layer 1 presence remains permitted in parallel under its existing constraint. Track C supplements Track B; it does not replace, widen, or reinterpret it.
>
> **The constraint.** New tables and migrations for attachments and derived views **only**. **No new column** on `conversations`, `messages`, `execution_events`, `deliberations`, `model_calls`, `personas`, or `budget_reservations` — a feature needing to touch those stops and waits for a ruling. The boundary is **semantic, not syntactic**: existing text-only turns behave exactly as they do now; existing Layer 0 tables do not acquire changed meanings; attachments live in their own substrate and reference existing message and model-call ids through sidecar or join structures. An additive, backward-compatible API surface for associating an attachment with a turn is permitted; changed meaning on an existing surface is not. **Backward compatibility is an acceptance requirement, not a preference**: existing conversation and message representations remain valid when attachment fields or associations are absent, and a text-only turn continues to execute through the existing conversation path without acquiring any new required attachment concept, state, or processing step. Track C may add an association path; it may not make attachments part of the mandatory core-loop contract. **Eligibility is unchanged and uses the existing gateway**: an image is external egress and is classified before it leaves; no provider gets a side door because vision needs one. **If multimodal pricing requires changing the budget or accounting doctrine** rather than feeding new measured usage through it, **STOP and rule before proceeding** — that is the strongest argument the gate has, and it is not being suspended.
>
> **What Track C does not do.** The seven closing points of §5 stay exactly as written. The counts stay at twenty and fifty. Layer 0 does not close until that single session is demonstrated as specified. **Vision behavior is not a gate criterion and does not reduce any count.** Both halves of the evidence distinction, recorded so this is not goalpost movement: **(1)** a conversation *made possible* by an attachment generates ordinary Layer 0 evidence when it genuinely exercises the unchanged Layer 0 mechanisms — a storyboard shown, two alternatives discussed, a consequential blind position formed through the existing machinery, judged with a substantive reason, is a real judgment and a real classification and it counts; the attachment made the conversation possible, it did not make the mechanism less real. **(2)** "Val described an image" never satisfies a gate criterion.
>
> **First deliverable: Substrate v1 as a contract, for Lord Armand's review before implementation.** Immutable original, attachment identity and hash, typed derived representations, parent/child provenance, processing and model provenance, structural locators, processing status and errors, and the rules for extending the representation model later. Contract only — no migrations, no application code — until he has seen it and ruled.

<!-- scope-ruling: 2026-09-16 -->
> **Amendment — 16 September 2026, Lord Armand. Local cognition infrastructure is authorised before the fifty, narrowly.** Local inference is now the priority for reducing ordinary cloud cost, so its *infrastructure* moves ahead of the WP-0.9 fifty-label gate — a deliberate scope opening, not a closure of anything. **Permitted:** the domain support a local provider needs (the `hosting` axis, `LOCAL_NO_METERED_COST` metering under validators that keep a zero rate illegal on any metered route); the LM Studio adapter (`lmstudio`, loopback-only, conversation-only, its own dialect, hidden reasoning discarded at the boundary); startup and secrets wiring (`VAL_LMSTUDIO_BASE_URL`, `VAL_LMSTUDIO_API_TOKEN` — required only where an `lmstudio` adapter is constructed, never by the production service while no local route is admitted); one registry entry `gpt-oss-20b-mxfp4-mlx-lmstudio`, **`NOT_ADMITTED`, no profile**, `qualification_targets = {PARTNER}` solely because the candidate lane requires a target to open — a target is not a profile, admits nothing, is consulted by no production path and is no evidence; tests; a live loopback proof and scratch-store candidate measurements as feasibility, dialect and telemetry evidence for a later owner decision on whether Partner-quality benchmarking should begin. **Not permitted, and not done:** closing or waiving the WP-0.9 gate, altering or manufacturing classification evidence, admitting the local model or giving it a production profile, replacing GPT-5.6 Sol, changing production routing, cloud escalation, adaptive scoring, skeptic/judge routing, tools of any kind for the local model, additional local models, or Restricted eligibility for local inference (a separate ruling not made). The measurements are not a Partner qualification packet. Recorded in `01-architecture.md` §5.4 (eligibility ruling) and §4 above (the deviation's expiry made precise).
>
> **Amendment — 16 September 2026 (later), Lord Armand. The local context preflight is exact; the cloud byte bound is unchanged.** The loopback proof showed the house byte bound (≈5× the real count on the local tokenizer) refusing a Val-shaped exchange at 32,768 from its second ordinary turn and refusing the *response* call of a consequential exchange on its first. The 8 September rule — the byte bound lives in the context-window preflight and the reservation — **stands unchanged for every `CLOUD` route.** For a `LOCAL_NO_METERED_COST` route only, ruled: (1) the preflight compares the **exact serialised prompt** — the already-loaded runtime's own prompt template applied to the exact message structure the request will carry, counted by that runtime's own tokenizer — plus the **whole requested output reserve (6,144, reasoning and visible text together)** against the **runtime's loaded context length**; `prompt + reserve ≤ loaded context` admits, anything else refuses before routing, reservation or transmission, and nothing is clamped, shortened or estimated to make it fit. (2) The runtime is authoritative for the window; the registry's 32,768 is nominal, and a disagreement is logged and recorded on the measurement row, never silently reconciled. (3) **Exact tokenization requires the official LM Studio SDK, and it is authorised for inspection only** — `lmstudio==1.6.0b1`, pinned exactly as a pre-release by explicit owner exception because 1.5.0 cannot authenticate to a token-protected server; used as a **read-only inspector** (`val_providers.lmstudio_inspector`): enumerate already-loaded instances, select the one intended deterministically or refuse (zero, ambiguous, unreadable identity or missing context length all refuse), apply the template, count, read the context length — **never** the SDK's get-or-load path, never a load, unload, reload, configure, generate or retry; the house token passed programmatically into a scoped client, never through the SDK's environment fallback, never rendered. Inference stays on the OpenAI-compatible HTTP adapter. (4) **Fail closed:** if the inspector cannot measure (runtime unreachable, instance not resident, ambiguous, refused), the byte bound applies as before with a warning; no estimator substitutes. (5) The adapter's post-call refusal of a prompt that filled the loaded context is kept. (6) **Early feasibility on the candidate lane:** before a consequential exchange's classification call, the known material (persona, envelopes, retained history, the turn) is measured; if it cannot fit the loaded context with the reserve, the exchange is refused before any call — classification, strip, blind position and response all skipped, nothing shortened. History collision is measured and reported, not compensated for. (7) **Parity is a hard gate:** on every admitted local call the inspector's count and the server's reported `usage.prompt_tokens` are recorded side by side on the measurement row (`runtime_diagnostics.preflight`, `runtime_diagnostics.parity`); a difference is logged as a warning, and in the proof any inexact parity STOPS the run for owner review. (8) Version provenance rides on every measurement (SDK version, API host, the instance's identity, loaded and maximum context, format, architecture). Nothing here admits the local model, changes production routing, or touches a cloud route. Evidence: `docs/reviews/VAL_Test_and_Evidence_Index.md` §70.
>
> **Amendment — 17 September 2026, Lord Armand. The local wire representation is canonical before both branches.** The Qwen3.8-27B proof (evidence index §72) showed the exact preflight and the inference request rendering different prompts from the same Core messages — 5,560 against 5,561 — because LM Studio's two ingress paths merged Core's structurally adjacent user pair (the record-state envelope, then the current turn) differently: the SDK's `Chat` into content parts a Jinja template concatenates with nothing, the chat-completions endpoint into one string with a blank line. Tokenization agreed; the seam was ingress canonicalization. Ruled: **the LM Studio adapter canonicalizes consecutive same-role messages into one message of that role, contents joined by exactly one blank line, before both the exact preflight and the inference request, from one shared construction** — so the runtime has nothing to merge on either path and divergence is removed by construction, never by calibration (no offsets, no per-template compensation, no near parity). Role-based and model-independent, never across roles, order and text otherwise unchanged, idempotent. **Core is unchanged:** assembly, the envelope, history, persistence and the visible-message representation still produce and hold the two adjacent user messages; only the local provider's wire form is normalized. The GPT-OSS exact-parity evidence stands for GPT-OSS and is re-proven under the new form; the raw-`ChatHistoryData` alternative returns to the owner only if this form cannot reach exact agreement. Reasoning-control declaration is a separate, later decision. Evidence: index §73.

<!-- scope-ruling: 2026-09-18 -->
> **Amendment — 18 September 2026, Lord Armand. A second local provider for candidate qualification, and the thinking declaration.** Local cognition infrastructure stays ahead of the fifty, as narrowly as before. **Permitted:** the `llamacpp` provider (a standalone llama.cpp server, loopback only, its own dedicated key; adapter, read-only HTTP inspector, startup and secrets wiring; `01-architecture.md` §5.4 ruling of this date); the configuration contract's `thinking_enabled`, `preserve_thinking`, `top_p` and `top_k`; migration `0023` widening the known-$0 clause by that provider's name; the measurement protocol carrying the call's output allowance so a runtime that counts whole request bodies counts exactly the body sent; one evaluation-only Gemma 4 31B entry once the runtime's identity is known — `NOT_ADMITTED`, no profile, `PARTNER` target for the candidate lane only; thinking-OFF and thinking-ON pre-suite proofs and, only if every gate passes, the frozen Stage A suite with thinking ON. **Not permitted, and not done:** admitting any local model, a production profile or route, replacing Sol, fallback, adaptive scoring, tools for a local model, Restricted eligibility for local inference, persisting or replaying hidden reasoning, any context substitution (the explicit 32,768 must be the server's actual context or the run stops), tuning sampling or thinking against the benchmark. **Template pin on that path (ruled later on 18 September 2026):** the server's active template must equal the pinned official template byte for byte, or equal it with exactly one terminal LF removed when the pinned file ends in exactly one LF (llama.cpp b10360 drops that byte reading `--chat-template-file`); nothing else is normalized, the raw, canonical and active hashes are all kept, and any other difference stops the run. This is not a whitespace-equivalence rule and it does not touch LM Studio. Mistral Small 3.2 is closed NOT_ADMITTED on model quality (evidence index §76); GPT-OSS and Qwen remain NOT_ADMITTED. **Gemma 4 31B (Q6_K, llama.cpp, thinking ON) closed the same day by owner/VAL disposition: NOT_ADMITTED as local Partner, Partner qualification closed** on model quality and operational suitability (evidence index §77–§78); the disposition is not a finding against the `llamacpp` provider, which stays as ruled infrastructure, and no further Gemma qualification cycle is opened.

<!-- scope-ruling: 2026-09-19 -->
> **Amendment — 19 September 2026, Lord Armand. Track C resumed: Attachment Substrate v1.2 implemented, and image input as the first slice of governed perception.** The substrate has been the governing contract since 2 September with nothing beneath it, and the storage question it blocked on was ruled on 7 September; the contract's implementation-blocked language about the sizing exercise is stale and the prerequisite discharged. **Permitted and done:** the six append-only tables through migration `0024`, with every §5 integrity rule structural rather than left to application care; deterministic image admission and derived `model_input_image` representations behind one pinned library (`pillow==12.3.0`, in policy beside the Restricted preflight, never in an adapter); **the provider-neutral ordered content-part boundary** — a turn is no longer a string but a sequence of parts, discriminated on `kind`, so audio and video become further members rather than a second boundary beside this one, and the text path is unchanged in every way that matters; image-input capability as a declared, dated, sourced configuration fact with the limits the reservation needs; `maximum_cost` widened to count image input and to fail closed on a route declaring none; the OpenAI adapter mapping parts to `input_image` in both modes with the cache boundary intact; the gateway owning admission, the atomic evidence commit, classification aggregation (strictest of the text's and every act's), transmitted-representation selection, the image-inclusive reservation and the call binding; the same ordered image set bound to the blind position and the final answer on a consequential turn; the API carrying attachments provider-neutrally and serving bytes by digest on loopback; the desktop composer and thread. **Current-turn visual binding (ruled this date):** an image is bound to its own turn and never silently retransmitted; the record-state envelope gains one additive field whose four states — `bound`, `earlier_only`, `none`, `uncertain` — are never collapsed, so *we discussed this image* can never read as *I can currently see it*, and the data model keeps a later explicit rebind possible without re-uploading or rewriting history. **The route (ruled this date):** `gpt-5-6-sol-medium` is the only image-capable production configuration; `opus-5-medium` and every local route are not. Verified against current provider documentation and then against the provider itself by four probes totalling $0.018072 of the authorised $0.50 (`docs/reviews/evidence/2026-09-19-sol-image-input.json`): the documented patch formula is exact within the budget, and an image the provider must resize billed one token above the formula's cap — so VAL never transmits an image the provider must resize. **Corrected 20 September 2026, on the owner's finding:** the bound is not a fixed long edge — 1,600 is right for a square and wrong for everything else, and a 16:9 frame at 2048x1152 is 2,304 patches, already inside the budget. `val_policy.attachments.fit_within_limits` finds the largest aspect-preserving size inside **both** documented limits (2,048 on the long edge and 2,500 patches of 32 pixels), never upscales, floors the short edge so the pair is a function of the long edge alone, and takes the largest feasible long edge — deterministic, so identical bytes and identical capability facts always yield identical derived bytes, which blind/final equality rests on. It does not reproduce the provider's own shrink: it simply never needs one. The provider's documented one-token billing rounding is carried as a declared **reservation margin**, never as part of the price. Provider facts and house policy are separate objects on the registry entry, because the provider documents no per-image size limit and the house's own ceiling must not be read as one. A `derive:model_input_image` attempt is recorded only when a derivation is actually attempted, and migration `0024` now structurally requires a succeeded derivation to name what it produced. **Request-level limits (corrected 20 September 2026, same first-party re-read):** the provider documents "Up to 1,500 images per request" and "Up to 512 MB total payload per request" **and nowhere defines what is measured against the second**. Those two are recorded as request-wide provider facts, with the documentation gap itself recorded as a fact; the house counts the complete data URIs it would transmit — the largest image-attributable quantity it puts on the wire, since raw bytes < base64 < data URI — recorded separately as a **house interpretation of an ambiguous provider fact**, never as something the provider stated. Core verifies both limits after every attachment is admitted and derived and **before any pixels leave the machine**, refusing with the limit, the measured state and which of the two was violated; it drops no attachment to make a turn fit and splits no turn across calls. The house per-image ceiling applies to the admitted **original** and is **50 MB** (owner ruling, 20 September 2026), raised from 20 MB so production material is not refused; the decoded-pixel safety bound is separate and unchanged. The reservation composes over every transmitted image, each with its own bounded term and its own one-token margin. **Not permitted and not done:** a multimodal classifier (the consequential classifier stays text-only for this slice, recorded as a limit); audio, video or document parts; a local image route; any local model download; automatic recall of attachments; Val-produced attachments; attachment editing or deletion. **WP-0.11 is unchanged and still open**, and still blocks Track C as an operational working-day capability and blocks visual conversations contributing gate evidence; one bounded genuine live acceptance sequence is authorised by exception, Sol only, capped at $0.50, and is not gate evidence.

<!-- scope-ruling: 2026-09-21 -->
> **Amendment — 21 September 2026, Lord Armand. GPT-OSS is admitted to the production text role, by explicit owner exception.** `openai/gpt-oss-20b` (MXFP4, Apple MLX, LM Studio, loopback) becomes **Val's default local text cognition / Partner**: ordinary conversation, reasoning, analysis, advice, judgment, writing, planning, creative collaboration, project discussion, technical assistance, the final user-facing response, and the consequential blind position. **Recorded as a new decision, not as a revision of an old one.** The Stage A record found this model NOT_ADMITTED against the frozen benchmark, on substantive reliability grounds and not on style — fabricated House Armand continuity, a false claim of system-log access, incorrect date arithmetic. That record, and the evaluation-only registry entry `gpt-oss-20b-mxfp4-mlx-lmstudio`, are preserved unchanged and unreinterpreted. The owner admits the model knowing them, and the three findings are carried on the new entry as declared production weaknesses in the words they were found in. Formal qualification status remains **NOT MET**; `QUALIFIED` is not set and nothing implies it.
>
> **Permitted and built:** a second registry entry `gpt-oss-20b-mxfp4-mlx-lmstudio-partner`, `PROVISIONALLY_ADMITTED`, declaring the `partner` profile and carrying the owner authorisation verbatim; the local runtime supervisor, so ordinary use never requires a terminal — the server is started if it is not serving, the model is loaded if it is not loaded, and the load **names the registered 32,768-token context** rather than inheriting the model's smaller just-in-time default, with one bounded attempt at each step and an honest failure otherwise; image capability as a routing filter, so a turn carrying images still selects the one image-capable partner route and Track C behaviour is unchanged; and **the stop before a paid Partner call**.
>
> **The stop, stated exactly.** Partner-class cognition — the calls in which Val herself thinks and speaks — runs on the house's own machine. When the local route cannot carry one, because the runtime is unavailable, because recovery failed, or because an enforced local boundary prevents it, **Core stops before transmitting to a metered Partner route** and says so. Cloud Partner escalation is the owner's decision and requires his explicit approval for that call; previous cloud use is not approval and there is no standing exception. This is enforced in routing and policy, never in persona wording. **In this pass the stop is built and the approve-and-retry interface is not:** the turn ends with the reason, and the next move is his. A dedicated escalation control is later work with its own ruling.
>
> **Out of scope and unchanged:** classification and stripping (§4 above); image routing, which keeps its existing Track C behaviour and gains no new standing exception; the WP-0.9 gate; the exact local context preflight, which is unchanged and remains authoritative at call time. **A consequence recorded rather than discovered later:** the production service now requires `VAL_LMSTUDIO_API_TOKEN`, because `start()` builds adapters for admitted providers and the local route is one; a service without it stops rather than running with Val's own route missing.

<!-- scope-ruling: 2026-09-22 -->
> **Amendment — 22 September 2026, Lord Armand. Qwen3.5-9B is admitted as Val's local visual-perception provider, and image turns stop transmitting pixels.** `lmstudio-community/Qwen3.5-9B-MLX-4bit` at immutable revision `b455506b0f574c74616dbcd56879bde38fafcff3`, 4-bit MLX, in the isolated MLX-VLM runtime, is admitted to the production role **VAL LOCAL VISUAL PERCEPTION PROVIDER** for **IMAGE PERCEPTION and VIDEO PERCEPTION only**. **AUDIO, TTS, speech generation and ordinary final-response cognition are not admitted and are not reachable** — the boundary's modality type has no audio member, and the entry declares one capability profile (`perception`) and no other. Admission follows the frozen two-case acceptance test, one run per case, under a generation configuration committed **before either case ran**: Case A PASS on the genuine Track C attachment under the repaired criterion (three people; top hat **and** cape both established in the criterion's own word; embracing; indoor living room; nothing material invented; no repetition pathology under the same untuned settings that defeated Qwen3-VL), Case C PASS on the repaired frozen fixture with delivery identical to the frozen sampling record, and **MACHINE FIT PASS** (zero new swap across 30 samples, never below 70% memory free, 18.31 GB headroom at worst, wired memory returned 13.10 GB → 2.97 GB, GPT-OSS reloaded afterwards at its registered 32,768-token window in 10.7 s through the production supervisor). One judgement call is recorded in the run's `RESULT.md` rather than smoothed over: Case C's answer establishes the traverse without using the word "centre", and why that is accepted where "coat" for "cape" was not. No exam repair was proposed or performed, and nothing was tuned.
>
> **Permitted and built.** A registry entry `qwen3-5-9b-mlx-4bit-mlxvlm-perception`, `PROVISIONALLY_ADMITTED`, carrying the owner authorisation verbatim, with `mlxvlm` ruled the **third LOCAL provider** (`01-architecture.md` §5.4) — a subprocess of this machine reading files from this machine, with no socket, no port and no URL. A new capability profile `perception`, which confers nothing else and to which nothing else confers access. A provider-neutral perception boundary in `val_domain.perception`, so Core depends on the contract and only the composition root knows what implements it. Migration `0025`: three append-only tables — `perception_runs` (the run, the artifact, the exact prompt, the owner's question, the frozen generation settings, $0, local), `perception_sources` (SOURCE → OBSERVATION, by digest, with a coherence trigger holding the act and the attachment to one file) and `perception_handoffs` (OBSERVATION → COGNITION). **No new column on the seven core tables**, and a perception run is deliberately **not** a `model_calls` row: that table accounts for calls that buy thinking, and this buys none. The fifth record-state visual value, `perceived`, additive beside `bound`, `earlier_only`, `none` and `uncertain`, carrying §20's note in substance.
>
> **Production image routing.** A normal image turn **no longer reaches a paid image-capable Partner because it contains an image**. Core perceives the media locally, once, before the classifier runs, and the cognition route receives an envelope of grounded observations rather than pixels — so the cognition call is an ordinary text call and the image-capable filter has nothing to filter for. **No standing Sol image exception was created**, and Sol's declared image capability, the Track C record and every failed candidate's record are untouched. Perception is targeted: the owner's **actual question travels verbatim**, framed by a stable instruction to report evidence and not to answer, advise, conclude, speak as Val, or invent. On a consequential turn perception runs **once** and the identical frozen envelope reaches both the blind position and the final answer; `perception_runs.message_id` is unique, so a second run for one turn cannot be written even by a mistake upstream, and two `perception_handoffs` rows naming one run are the durable proof. A later text-only turn uses the stored observation and **does not reinspect**; the cognition provider cannot reach for raw media on its own.
>
> **The stop, in the same words as the Partner stop of 21 September.** When local perception cannot be done, after the adapter's one bounded recovery attempt, the turn **fails closed and says so**. The media are not sent to a paid image-capable provider instead, and no cognition call is made at all. Enforced in Core and policy, never in persona wording.
>
> **Automatic local runtime.** Ordinary image use requires no Terminal, no manual Python command, no manual model load, no manual server start and no manual unload. MLX-VLM is a library, not a server, so the smallest reliable mechanism is one subprocess per run: it loads the admitted artifact, perceives, and exits — and **exit is the release**, which is how the visual model and the cognition model take turns rather than compete. The argument vector is two constants; the request travels on **stdin as JSON**, so nothing a model wrote, a document contained or a tool returned can become an argument. Val's production Python environment and its pinned dependencies are untouched.
>
> **Amended later the same day, on the owner's execution order: video and audio ingestion, and a second specialist.** Perception becomes a **modality-specialist** subsystem rather than a visual one. `Qwen3-Omni-30B-A3B-Instruct-GGUF` at immutable revision `6e35a28f4a19b18730f8949b0c579c6429649ab8`, Q4_K_M with its Q8_0 projector, on the official llama.cpp v0.4.1 build 10964 commit `b29c606e2` it was measured on, is admitted as **VAL AUDIO-PERCEPTION PROVIDER ONLY**, on the frozen Case B evidence already on record (PASS: all seven required facts in 3.5 s through the model's own native multimodal path — no Whisper, no separate transcription model, no cloud service, no pre-transcribed text). **Its earlier overall candidate failure stands unreinterpreted**: that candidacy failed on VIDEO, and the failure is carried on the entry as a declared weakness. `llamacpp-omni` is the fourth ruled LOCAL provider, kept distinct from the keyed loopback `llamacpp` server because they are different binaries with different contracts.
>
> **Modality is declared, never inferred.** `ModelConfig.perception_modalities` and `PerceptionProvider.modalities` both name what a route is admitted for, and routing consults only those: Qwen3.5 carries `image, video` and never receives a recording; Qwen3-Omni carries `audio` and never receives a video, though its projector contains a vision encoder — a fact about the artifact, not a permission. Holding the `perception` profile says nothing about *what* is perceived, and a perception entry declaring no modality is refused at construction.
>
> **Owner-facing ingestion, end to end.** `video/mp4` and `audio/wav` join the governed attachment path — the existing blob, attachment and act tables, with no new media product beside them. Admission reads the bytes and says **how far it actually got**: a WAV is parsed by the standard library (`decoded_header`, with its true duration), an MP4 container is walked for `ftyp`, brand and `moov` (`container_structure` — a container check, not a decode, recorded as one), an image is genuinely decoded (`decoded`). Migration `0026` is additive: `audio` joins the modality vocabulary, and `perception_sources` gains nullable `duration_seconds` and `verified`. The desktop attaches, previews and plays all three, and the Tauri CSP gains `media-src` for the governed loopback origin — without it a `<video>` or `<audio>` is refused by the webview before any request, which looks exactly like a broken file.
>
> **The audio doctrine is the visual one in the verbs that fit a recording** (§10): report the grounded information the recording conveys, relevant to the owner's verbatim question; do not answer, advise, conclude beyond the recording, speak as Val, or invent; say when the recording does not establish something. The exact prompt is persisted. **Audio is never reported as visual input:** the record-state envelope gains an additive `audio_input` field, present only when the conversation has actually carried audio, and `visual_input` is neither renamed nor reinterpreted.
>
> **Mixed audio-and-visual turns fail closed, by name.** The two specialists are different models, and running both over one turn — two frozen observations, both grounded identically through a consequential deliberation — is a redesign rather than a wiring job. It was ruled out for tonight rather than half-built. The refusal names mixed audio-and-visual attachment as the reason and states that single-modality turns are supported; a generic failure would leave the owner guessing at a rule nobody wrote down.
>
> **The fall-through is closed.** An admitted perception route whose local provider is not wired now **fails closed** rather than reverting to the historical raw-media path: a missing local component is a misconfigured house, not permission to spend the owner's money on a paid visual provider. `bound` keeps its meaning and its historical rows exactly, and the Track C machinery is unchanged and still proved — in the world it belongs to, which those tests now state explicitly rather than inherit from the registry of the day they were written.
>
> **Still not permitted and still not done:** TTS, voice cloning, speech output, avatar speech, Talker or token2wav — no such component is downloaded; Qwen3-Omni on image or video, or as final-response cognition; Qwen3.5 on audio; a multimodal classifier; any new model qualification. **WP-0.11 is unchanged and still open.**

> **Amended again the same day: Val has a local voice, designed rather than borrowed.** The earlier Qwen3-TTS Base attempt was closed FAILED at its pre-download rights gate — every VAL voice recording on the machine is ElevenLabs Output (five files, each carrying `kMDItemWhereFroms = https://elevenlabs.io/`), and no agreement, enterprise agreement or written permission anywhere in the owner's records authorises using that Output as another AI model's input. **That finding stands and is not reopened.** The blocker is removed not by reinterpreting it but by removing the dependency: the reference is now generated locally.
>
> **Permitted and done.** `mlx-community/Qwen3-TTS-12Hz-1.7B-VoiceDesign-8bit` @ `f90d617701d9f7f4ca499291e0b57f2b3c2fd2ee` (Apache-2.0, 3.08 GB, 8-bit MLX) generated **one** reference recording from the owner's frozen textual description of Val's voice — no audition batch, no seed search, no regenerate-until-good loop. `mlx-community/Qwen3-TTS-12Hz-1.7B-Base-8bit` @ `e7dd0585652209fa0d7783659aad4e8a324de11c` (Apache-2.0, 3.10 GB; `model.safetensors` verified against the source hash `b965c581…`) is admitted as **VAL LOCAL SPEECH-OUTPUT / TTS PROVIDER**, speaking new text conditioned on that reference through Qwen3-TTS's in-context-learning path. `mlxaudio` is the **fifth ruled LOCAL provider**, in its own dedicated runtime (`~/.val-runtimes/mlx-audio-venv`, mlx-audio 0.5.5) so that neither Val's pinned environment nor the admitted *visual* runtime is disturbed. A new capability profile `speech`, the narrowest in the house: a route holding it carries no conversation, no blind position, no classification, no strip, no title and no perception.
>
> **The reusable identity anchor.** The clone conditioning — the speech tokenizer's codes for the reference and the reference text's token ids, MLX-Audio's equivalent of Qwen3-TTS's `create_voice_clone_prompt` — is computed once and written to `~/.val-voice/val-clone-prompt.npz`, then reloaded on every later utterance. **VoiceDesign never runs in production**: it produced the reference and its role ended, because designing the voice afresh per sentence is how a speaker identity drifts. The production smoke reused the byte-identical prompt the acceptance generation created (`4e0acd9c…`), which is the proof rather than the intention.
>
> **What Core decides and what the provider decides.** Val Core decides what is said; the provider decides only how the already-final text is spoken. `speak_message` reads the persisted `val` message and hands its content over verbatim — there is no rewriting, summarising or style parameter, and a user message is refused because Val speaks her own words. Migration `0027` adds two append-only tables: `speech_voices` (the voice as an identity, written once, carrying its origin **and what is not claimed about it**) and `speech_generations` (one utterance, holding Val's finished words verbatim beside the waveform's digest).
>
> **The stop.** When local speech cannot be produced, after one bounded recovery attempt, the request **fails closed**. **ElevenLabs is never invoked automatically** — it remains preserved historical/external VAL voice continuity and may later be an explicit owner-selected service, but it is not a fallback, and a fallback nobody chose is a decision nobody made.
>
> **Acceptance, run once each and never tuned after hearing the result.** The VoiceDesign reference: 24 kHz mono, 8.24 s, peak 0.546, **zero** samples at full scale, longest interior quiet 0.843 s, every word present on local Qwen3-Omni content verification. The Base different-sentence generation: 6.64 s, peak 0.483, zero clipping, no interior silence, word-for-word exact. The production smoke through the real composition root: 2.88 s, zero clipping, exact words, **zero provider calls in the interval**, $0. Waveform properties were measured from the signal with thresholds stated before measuring, never inferred from a transcript.
>
> **Voice identity is NOT model-verified, and is not claimed to be.** The accurate statement, recorded on the registry entry and in `speech_voices.identity_claim`: *Val's local voice was independently created from her owner-defined textual voice identity using Qwen VoiceDesign, then stabilised for repeated local speech through Qwen Base voice cloning.* It is **not** an acoustic clone of the historical ElevenLabs voice, no similarity score was manufactured, and no speaker-recognition model was added to grade it. Final identity is the owner's listening judgement.
>
> **Amended 23 September 2026, by owner authorisation: the production voice is Val's established voice.** Lord Armand authorised his own ElevenLabs Voice Design creation — an original synthetic voice made for Val, not a clone of any real person — as **inference-time reference conditioning** for this local system, specifically for conditioning, the verification sample and the production smoke, and **not** for training, fine-tuning, benchmarking, comparison or dataset collection. He records that he knows ElevenLabs' published terms restrict AI-model use of Output, that he judges private zero-shot reference conditioning materially distinct from model training or development, and that he accepts the contractual ambiguity. **The ruling supersedes the earlier prohibition for this implementation, and this house did not adjudicate it**: it is carried verbatim on the registry entry and in `speech_voices.identity_claim`, stated as his interpretation.
>
> The production voice is now `val-established-v1`. **`val-local-v1`, designed on 22 September, is preserved unaltered with its evidence** — nothing was erased, and the registry entry records both facts rather than replacing one with the other. The authorised recording (`VAL's Voice.mp3`, `dfea9cde…`) and the bytes the model reads (a deterministic 24 kHz mono transcode, `c5ebe0c7…`) are recorded as two digests, because they are two objects. **The transcript is machine-read by the admitted local Qwen3-Omni route, not owner-supplied** — no Whisper, no cloud STT, and never the TTS runtime's automatic transcription — and that provenance is carried as a declared weakness, since a misheard word makes the conditioning less faithful.
>
> **A defect found and closed before either run:** the adapter held one clone-prompt path for all voices, and the library keys its ICL cache on the reference, so a second voice could have been conditioned on the first one's stored codes — Val in the wrong voice with every digest still looking right. The path is now derived from the reference digest, and a test holds it there. **Verification sample and production smoke both PASS**, both at $0, the smoke reusing the verification sample's clone prompt byte-identically, with VoiceDesign not running. What is established is that speech is conditioned on the reference; **how closely it resembles her remains his listening judgement and is not model-verified.**

> **Not permitted and not done:** TTS voice cloning from any third-party generated audio; ElevenLabs, Higgsfield or any external voice as conditioning input; a second VoiceDesign generation or a variant audition; voice cloning of a real person; avatar, lip-sync, LiveAvatar or voice-mode UX; any change to Val's production Python environment.

> **Not permitted and not done:** audio of any kind, TTS or speech generation; Qwen3.5 as Val's final-response model or on any other profile; a multimodal classifier (the consequential classifier stays **text-only**, recorded as a limit); Restricted eligibility for local perception; any change to the WP-0.9 gate, to classification or strip routing, or to the fifty; cleanup or connection of the MiniCPM-o, Qwen3-Omni, Qwen2.5-VL or Qwen3-VL artifacts, which are left on disk and wired into nothing. **The video Core boundary, stated honestly:** video perception is admitted and demonstrated, and **owner-facing governed video ingestion does not exist** — no owner video reaches it, no video attachment path was built for it, and nothing here claims otherwise. **WP-0.11 is unchanged and still open.**

<!-- scope-ruling: 2026-09-23 -->
> **Amendment — 23 September 2026, Lord Armand. Live voice mode, work package 1: service-side voice input is authorised and built.** Val hears. The boundary is `in-memory PCM → local whisper.cpp + Silero VAD → provisional transcript state → final transcript → canonical Val user turn → the existing Val Core`, and nothing beyond it. The order splits the Voice-mode implementation into bounded work packages; this is the first, and it is the input half only.
>
> **The governing sentence: a spoken turn is an ordinary turn.** There is **no voice cognition route**, no voice-specific classification and no voice exemption from consequence handling. A settled utterance is submitted through `val_gateway.deliberate.send` — the same door a typed turn uses — and therefore through project resolution, classification, the strip and blind machinery, memory, recall, the record-state envelope, the persona, routing, budget and persistence, unchanged. **Production GPT-OSS remains MEDIUM and is untouched.**
>
> **Permitted and built.** The recognizer helper in the dedicated voice runtime (`~/.val-runtimes/voice-venv`, numpy alone), reaching the pinned whisper.cpp **v1.9.4 / `927cfce34f31707e17f2bff35c349632fb9e2c3a`** through `ctypes` — **no production Python dependency is added and no pin is changed**; a framed stdin protocol (`[u32 kind][u32 length][payload]`) so audio never becomes a command-line argument; a production recognizer adapter that **owns no microphone**, receives audio from a caller, and refuses a model that is not the admitted one (`ggml-small.en` `c6138d6d…f9c41e5d`, Silero VAD v6.2.0 `2aa269b7…f7fb6987`) before anything listens; a voice-session state machine; migration `0028` with three append-only tables; and the smallest service contract the later desktop package needs, on the existing transport, under the existing CORS grant and the existing loopback-only bind — **no new origin, no new bind, no new header, no upgrade, no token.**
>
> **Provisional is not final, and the distinction is structural.** A rolling guess is mutable session state: not a message, not conversation history, not recall evidence, not Partner context, not indexed. The domain carries two types rather than one with a flag, so promoting the wrong one is a type error. The classifier therefore sees settled text and never a guess, because a guess never reaches `send` at all. **Consequential behaviour operates on final transcription only, and speaker identity is not authorization** — there is no wake word and no speaker recognition.
>
> **Raw audio is never persisted, anywhere.** No audio column of any type exists on any of the three new tables; no temporary WAV, MP3 or spool file is written; audio is volatile session state released the moment an utterance is settled. Audio never enters conversation history, attachments, recall, evidence or project assets.
>
> **The text-only recovery journal.** A crash during an unfinished spoken utterance loses no words, and recovered words are **provisional, never canonical**: `voice_recovery_journal` holds text only, is superseded by appending rather than by update, can be `provisional`, `superseded`, `abandoned` or `interrupted` and **never `final`**, and is structurally excluded from recall — recall reads `messages_current`, of which it is no part. A guess a restart finds open is reported back labelled `interrupted` and is never silently promoted into something he said.
>
> **Resume before delivery.** When he pauses, the utterance endpoints, and he carries on before Val has said anything aloud, that is one thing he meant to say, and it becomes **one** owner turn. Before submission the halves are joined and nothing needs cancelling. After submission and before delivery the canonical wording is corrected through the existing append-only revision machinery, noted *resumed before delivery* — never by UPDATE, never by deleting the first turn. **Where the message anchors an enforced blind position or a deliberation the correction is refused and the resumed speech becomes its own turn**: continuity is not worth falsifying evidence that was valid when it was produced.
>
> **Endpointing, recorded in governed configuration** and carried on every session row: threshold 0.5, minimum speech 220 ms, minimum silence 650 ms, speech padding 80 ms, and a house ceiling of 30 s on one utterance. One reasonable starting configuration; **no tuning campaign was conducted.** Two facts established by measurement and recorded rather than assumed: the Silero VAD **must** run on the CPU, because `use_gpu = True` aborts the process in this build, and costs a real-time factor of 0.003; and provisional decoding is gated on **audio time, not wall-clock time**, because wall-clock gating yields no provisional events at all when audio arrives faster than real time.
>
> **A stated limitation, not hidden:** a voice session's durable row is written on first need, because a session belongs to a conversation and a new chat's conversation is created by its first spoken turn — so **the first utterance of a brand-new chat has no crash recovery**, and every later one does.
>
> **Not permitted and not done:** TTS delivery, speech segmentation, barge-in after Val begins speaking, playback, the desktop Voice button, the AudioWorklet, desktop microphone capture, echo-cancellation acceptance, the avatar, the camera, latency optimisation, low-reasoning GPT-OSS measurement, speculative decoding; any cloud speech recognition, which is unreachable from this path; any Qwen3-Omni fallback for hearing him speak, which is a different act from perceiving a recording he attached and stays that way; any change to GPT-OSS, the persona, Qwen3-TTS, the WP-0.9 gate, or WP-0.11, which is unchanged and still open. Evidence: `docs/reviews/VAL_Voice_Input_WP1_Record.md`.


<!-- scope-ruling: 2026-09-24 -->
> **Amendment — 23–24 September 2026, Lord Armand. Live voice mode, work package 2: Val speaks, and stops when he does.** The output half is built and shipped: `Core's visible response stream → the exact speech segmenter → the established local voice → progressive ephemeral audio → delivery-state truth → interruption`. The input half of work package 1 is unchanged and was not reopened.
>
> **The bounded reasoning-effort measurement came first, and decided nothing.** `gpt-oss-20b-mxfp4-mlx-lmstudio-low` is registered **`NOT_ADMITTED`, no capability profile**, carrying a `PARTNER` qualification target only because the candidate lane needs one to open, so that a latency measurement could put two efforts on one identical request path; the production entry was not touched and a test asserts mechanically that the two measured entries differ in `reasoning_effort` and nothing else. Three local calls at a known **$0** on the governed ordinary evaluation path — **no metered classification, strip or blind-position call, and no live-store conversation** — one warm-up at MEDIUM, recorded truthfully under its evaluation configuration and excluded from the comparison. Measured on A1: first Core-visible text **10.754 s** (MEDIUM) against **8.013 s** (LOW), **25.5 %**; first speech-safe boundary **11.108 s** against **8.206 s**, **26.1 %**; hidden reasoning 195 tokens against 20. Prefill is **NOT DIRECTLY OBSERVABLE** on this runtime and no figure was invented for it. §2.5 triggered on the percentage conditions and not the absolute one, so the paired current-system Stage A comparison ran: the frozen twelve-task corpus of `60b65b7` once at each effort under persona v1.9, MEDIUM **41/44** frozen checks and LOW **40/44**, with LOW's two losses being a **correction-preservation failure** (it put the withdrawn venue back into an invitation) and an instruction-boundary failure, and MEDIUM's one loss being the check matching italics rather than a stage direction. No score, no self-grading, no cloud judge, no winner. **PRODUCTION TEXT COGNITION REMAINS `gpt-oss-20b-mxfp4-mlx-lmstudio-partner` AT MEDIUM**, pending his ruling; the MEDIUM baseline run is a comparison and reopens neither MEDIUM's qualification nor its admission.
>
> **The exact speech segmenter.** Every segment is a contiguous slice of the visible response, the slices are in order, and together they cover it completely — asserted at every chunk size from 1 to 39, because a boundary rule that only works when a sentence arrives whole is not a boundary rule. Nothing is paraphrased, summarised, rewritten, reordered, normalised, padded or dropped. Boundaries are the earliest real sentence end or `;`/`:`, with a comma, dash or word-gap cut when a sentence grows past 220 characters and a hard ceiling at 420; never mid-word. **Text Core withheld stops speech rather than being filtered**: a verdict or hidden-reasoning marker raises, because its arrival means a boundary upstream has failed.
>
> **Progressive delivery in the established voice.** `val-established-v1` on the existing Qwen3-TTS Base/ICL path with the existing per-reference clone prompt. VoiceDesign does not run, no new voice was created, the reference and its transcript are untouched, ElevenLabs was not called, no cloud TTS was called and no other TTS was benchmarked or compared. **Native per-token streaming was not available on the installed route and mlx-audio was not upgraded to obtain it**, so delivery is phrase-level progressive; a route that cannot stream still speaks, from the persisted answer, and a stream that does not match the record **fails with that named reason** rather than speaking words the record does not hold.
>
> **Live speech is ephemeral.** Migration `0029` makes that representable rather than fabricating a path: `speech_generations.audio_path` becomes nullable with a companion `audio_retained` held to it by a check constraint, so a row can neither claim a file that does not exist nor hide one that does; historical qualification samples keep their paths and read exactly as before. The digest, duration and byte count stay, because they remain true about audio that really was produced. No live waveform, MP3, PCM or encoded stream is retained, and the delivery module contains **no file write at all**.
>
> **Delivery truth.** `speech_deliveries` is append-only, one row per transition, each carrying the **exact prefix delivered at that moment**; a delivery's state is its highest-numbered row, UPDATE and DELETE are both refused, and the five states — `not_started`, `started`, `completed`, `interrupted`, `failed` — are distinct, with a reason required for the last two and forbidden for the others. **The delivered boundary is the FIRST audio, not the last**, and crossing it closes work package 1's resume-before-delivery merge. **Later context does not assume he heard the rest**: the record-state envelope gains an additive `spoken_delivery` field, present only when an answer ended short, naming how much he heard against how much was written, with a note that says plainly not to assume he knows the part he did not hear. The assistant message is never rewritten and no second conversation history exists.
>
> **Interruption.** The recognizer hearing him while delivery is live stops the sink first, discards the queue, starts no further synthesis, and records the exact prefix. **Nothing already executed is undone.** Service-side cancellation — the recognizer's `speech_start` reaching delivery control to the in-memory sink stopping — measured **0.008 ms** in the deterministic test and in the production smoke, against a 300 ms target. **That is not a claim about when a physical Mac speaker falls silent.** §14 is proved logically only: the sink's executable code contains no `feed` and no recognizer reference, and the session feeds the recognizer from exactly one place; **no claim is made about real speaker echo triggering the microphone.**
>
> **The concurrent machine-fit gate and the end-to-end smoke, in one run — PASS.** Everything real but the database. Turn 1: the frozen fixture transcribed exactly, Val's answer spoken in two segments that **reconstruct it exactly**, delivery `completed`, 106 characters delivered, the sink holding nothing afterwards. Turn 2: **barge-in through the real recognizer**, `interrupted`, **75 of 136 characters heard**, the exact prefix on record, and the next turn told so. GPT-OSS resident at 32,768, the whisper.cpp session alive, Silero VAD on the CPU and Qwen3-TTS speaking, all at once: lowest free memory **25.1 %**, **zero new swap**, swap after equal to swap before, nothing killed, all three components functional afterwards. §16's reportable intervals, labelled as the order requires: **end of input-fixture speech → first audio ready at the production delivery boundary 38,708 ms**, **first Core-visible text → first audio ready 8,578 ms**; the physical first-audible timestamp is work package 3's and is not claimed. Negatives: **135** columns scanned and none holds the owner's audio, no audio path written, every generation ephemeral, local speech **$0**, zero cloud STT, zero cloud TTS, zero ElevenLabs generation calls, zero VoiceDesign runs; total provider cost **$0.001692**, being two cloud classification calls.
>
> **The work package 1 recognizer integrity defect is repaired.** A digest was cached against the file's path, so a model replaced after the first successful verification was accepted unread. It is now cached against `st_dev`, `st_ino`, `st_size`, `st_mtime_ns` and `st_ctime_ns` with the admitted digest; any change rehashes, a mismatch refuses before the recognizer starts, and the file is re-stat'ed after hashing. The test that ended in a bare tuple expression and asserted nothing is replaced by five that assert refusal, including a same-size substitution with the modification time restored.
>
> **Speculative decoding — read-only check, NO COMPATIBLE PATH.** No download, no configuration or runtime change, no inference call, no restart. The machine holds no speculator of any kind — no EAGLE-3-style or DFlare-style GPT-OSS draft model and no smaller `gpt_oss`-architecture model — and Val's pinned OpenAI-compatible chat-completions contract carries no speculative field, so adding one would change the established inference contract.
>
> **Not permitted and not done:** any change to production reasoning effort, the persona, Qwen3-TTS or the established voice; admitting LOW; a cloud judge; tuning LOW against the twelve cases; modifying the frozen prompts or checks; the desktop Voice button, AudioWorklet, device capture, speaker-output acceptance or the acoustic echo-cancellation test; barge-in after audible delivery in the room; avatar; camera; wake word; speaker recognition; biometric identity; any requalification of Whisper or Qwen3-TTS; any search for another STT, TTS or cognition model; a permanent live-audio archive; any weakening of append-only evidence. Evidence: `docs/reviews/VAL_Voice_Delivery_WP2_Record.md`.


<!-- scope-ruling: 2026-09-24 -->
> **Amendment — 24 September 2026, Lord Armand. Live voice mode, work package 3: the desktop speaks and listens, and a spoken conversation never leaves this Mac.** The owner-facing half is built: the Mac's microphone and speakers, owner-only activation, true mute, the global mute shortcut, physical playback and physical barge-in — and, ruled at the same time, a new privacy boundary that governs the whole house.
>
> **Microphone activation belongs to the owner alone.** Val may never activate it: no model, persona instruction, Core decision, tool, API event, automation, timer, wake word, idle behaviour, app launch, restart, machine wake, conversation restore, error recovery, recognizer event, TTS event or future provider may acquire a device. The only valid activation events are explicit owner gestures in the desktop — the visible Voice control to start a session, and the visible microphone control or the global shortcut to unmute one that is already active. **The shortcut cannot turn Voice on from off**, and is unregistered while Voice is off. There is no wake word, no stored "Voice was enabled" preference and no automatic resume. **Physical speaker output is the owner's too**: with Voice off, no generated speech may be played at all. Voice is off after launch, restart, crash-relaunch, machine wake, sleep/resume, a new conversation, a conversation switch, a fatal transport error and a permission revocation; a failure may move Voice toward MUTED or OFF and **never** toward a live microphone or an enabled speaker.
>
> **THE LIVE-VOICE SEAL — a new owner privacy ruling.** A live microphone transcript must never be sent, in whole or in part, to any cloud model provider, classifier, preference-strip route, blind-position route, judge, remote tool, web search or other off-machine destination — **not on the turn it was spoken, not by a later typed turn in the same conversation, and not by being recalled into a different one**. It is enforced structurally in the domain, policy and gateway, never by persona wording. Two layers: a **transient** layer, under which a conversation with an open Voice session is local-only for every request it makes, typed or spoken, whether or not anything has been said; and a **durable** layer, applied only when live-microphone-derived text first becomes a canonical message, **in the same database transaction as that message**, by every route to canonical — an ordinary finalized utterance, the resume-before-delivery merge, and an owner-adopted recovered fragment. A Voice session that produces no canonical spoken text leaves the conversation **unsealed**. There is no unseal control. **The seal travels with recall**: any request carrying content recalled from a sealed conversation is itself local-only, wherever it belongs — and sealed conversations are **not** excluded from recall, because a spoken conversation is remembered exactly as a typed one is.
>
> **The seal is a dedicated local-only egress fact and deliberately NOT `Classification.RESTRICTED`.** Restricted would either strand a spoken turn with no eligible route, the local configurations being registered Restricted-ineligible, or force that eligibility to be widened — and whether local inference may carry Restricted content is a reserved owner ruling that has **not** been made and was not made implicitly here. Restricted also carries recall and handling protections that would change how spoken conversations are remembered. So a voice turn keeps its ordinary classification (Protected), **no registry configuration changed and none was added**, and the seal governs egress only. Migration `0030` adds `conversation_egress_seals`, append-only, one row per conversation, UPDATE and DELETE both refused.
>
> **The owner-accepted consequence.** Consequentiality classification and preference stripping run only on cloud structured routes, so a sealed conversation may not use them: in a sealed conversation — including every later typed turn in it — the consequential machinery, the blind-position independence protection, web search and remote tools are unavailable. He accepts that trade knowingly for the guarantee that no live-voice transcript leaves the Mac, and it is **not** worked around by substituting an unqualified local model for a cloud structured role. Restoring it requires a separately qualified local structured route, which is later work. **The record says NOT RUN, never a fabricated negative**: `classifications` gains `not_run_reason`, and a constraint holds such a row to no attempts, no verdict, no calls and no failure resolution. **The finding required before the rule was written:** the consequentiality classification currently gates the reasoning path of the response and nothing else — Layer 0 has no action with effects outside the conversation, no tool contract, no MCP client and no web search — so nothing can execute on NOT RUN today, and `val_policy.consequence` installs the gate so that the first executor to arrive fails closed. Any existing owner-confirmation requirement stands; NOT RUN never waives one.
>
> **A stated ordering fact.** An unsealed conversation's turn classifies on its cloud structured route **before** recall is assembled, so that call carries the newly typed message alone — no history, no recall, no envelope, no voice-derived context — and the sealed phrase cannot reach it. The escalation applies to the call that actually carries recalled content, which routes locally. The alternative, sealing every conversation in a project as soon as one was spoken in, is explicitly ruled out: a new conversation that draws in no sealed content is the clean boundary back to ordinary egress.
>
> **Devices belong to the desktop.** Capture is `getUserMedia` plus an AudioWorklet in `apps/desktop`, and a structural test asserts **exactly one call site in the whole application**. No MediaRecorder, no ScriptProcessorNode, no temporary file, no shell capture, no API-side capture daemon, no model-reachable device path. **Mute releases the device**: every track stopped, nodes disconnected, stream dropped, context closed, module URL revoked, and no sample accepted afterwards — not `track.enabled = false`, not a stream held open with its samples discarded, not a warm microphone. The macOS orange indicator is the owner's ground truth and an application icon cannot substitute for it. Unmute is an explicit gesture that reacquires; a failed reacquisition stays muted and does not retry. Mute while Val is speaking leaves her speaking. Migration `0030` also adds `speech_playbacks`, append-only, keeping `available_to_desktop`, `playback_started`, `playback_completed`, `playback_interrupted` and `playback_failed` apart, because bytes handed to a desktop are not sound in a room.
>
> **Not permitted and not done:** any change to production reasoning effort, the persona, Qwen3-TTS, the established voice or the registry's eligibility; admitting LOW; classifying voice content Restricted; substituting an unqualified local model for a cloud structured role; durably sealing a conversation merely because Voice was turned on; cloud STT; cloud TTS; ElevenLabs generation; VoiceDesign; an external voice transport; a hidden persistent microphone; a wake word; avatar; camera; video mode; reopening WP1, WP2, GPT-OSS qualification or speculative decoding. WP-0.11 is unchanged and still open. Evidence: `docs/reviews/VAL_Voice_Desktop_WP3_Record.md`.


<!-- scope-ruling: 2026-09-12 -->
> **Amendment — 12 September 2026, Lord Armand. Conversation management is authorised before Layer 0 closes, narrowly.** Conversation management is now a demonstrated daily-use requirement and supports the genuine use through which the remaining gate evidence must accumulate, so it moves ahead of the gate as a **gate-enabling daily-use capability**, limited to the operations ruled in the §2.1 amendment of this date: **rename, archive/unarchive, append-only user-message revision and retraction, conversation Remove/reinstate, and explicit append-only scope transition.** It supersedes item 1 of the post-gate order for those operations only. It does **not** authorise document comprehension, audio or video, general memory redesign, the books or Layer 5 learning, provider or routing changes, classification changes, deliberation redesign, tools or MCP, voice or avatar, mobile, broader visual UI redesign, or true content erasure. The Layer 0 append-only and evidence invariants remain binding, the seven closing points of this section stay exactly as written, and edits, removals, archives and moves made in use are product evidence, never Layer 0 judgment evidence by the fact of having occurred. Staged: (1) rename and archive; (2) message revision and retraction; (3) conversation Remove/reinstate; (4) explicit scope transition — each its own commit with CI green before the next.

---

## 6. Explicitly deferred

Recorded so it is not added under pressure of seeming incomplete:

| Deferred | Arrives |
|---|---|
| Voice, avatar, local inference | Layer 1 |
| **Processing Restricted content** — requires local inference (§1.1) | Layer 1 |
| **Per-content data classification and per-call eligibility checks** — Layer 0 satisfies invariant 17 structurally (§1.1); tools break that guarantee | Layer 2 |
| MCP, Tool Registry, any tool at all | Layer 2 |
| **Vision** — images reaching the model *as images*, with sight recorded per exchange (`01-architecture.md` §2.2). Re-scoped 1 September 2026: attachment-scoped sight arrives **post-gate** (§5 amended order); folder-scale filesystem reading stays Layer 2 | Post-gate / Layer 2 |
| Roles, agents, self-evaluation, standing adversary, prediction ledger scoring | Layer 3 |
| Temporal, graduated budget thresholds, reserve, cost dashboard | Layer 3 |
| Outbox, audit chain, approval flow, versioned writes, risk-tier machinery | Layer 4 |
| Lesson distillation, promotion, the books as readable artifacts, success models | Layer 5 |

Layer 0 has no actions, so it needs no protection against them. Building that protection now is the failure the whole layering exists to avoid.

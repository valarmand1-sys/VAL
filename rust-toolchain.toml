# Pinned Rust toolchain for the Tauri desktop shell.
# rustup reads this file automatically; no manual `rustup default` step is needed.
# Resolved version and source: docs/TOOLCHAIN.md
[toolchain]
channel = "1.97.1"
components = ["rustfmt", "clippy"]
profile = "minimal"
